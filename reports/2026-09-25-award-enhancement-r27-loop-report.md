# 获奖提升 · R27 汇报：award_demo 自推进闭环（推荐→取单→执行→验收→再推荐）

> 日期：2026-09-25｜目标 pillar：自驱式 / 计划性增强（评审演示）
> 方式：fist-mbt 自驱式获奖概率提升链路；本轮为 demo 闭环化（纯脚本，不新增工具/测试）。

## 结果摘要
- 把"自驱取单"串成**可看一整圈的自推进闭环**：`award_demo.py` 新增第 ⑪ 步——对接续取中的任务执行→提交→验收，再重新 `task_triage`，实证"**推荐→取单→执行→验收→再推荐**"整圈跑通（实证：T0r106.2.1 按推荐取单并完成，重推荐即指向下一条 T0r106.2.2）。
- 这是无人值守"按推荐续取"（roadmap #1）的具象、可复现演示，评审一条命令即可看到自驱如何推自己往前走。
- 纯脚本组合，**无源码/工具/测试数变化**（79 工具 / **220/220**）；结尾 `cleanup --check` **CLEAN**。

## 资源消耗
- 修改：`scripts/award_demo.py`（第 ⑪ 步：execute→submit→verify→re-triage）；`memory/2026-09-25.md`（R27）。
- 验证：实跑 award_demo 全链 PASS + CLEAN。

## 任务分配记录
- 直接实现（指挥官终审制）：复用既有工具串联闭环；实跑验证。

## 遗留风险
- 演示闭环按"单条推荐→执行→验收"推进；"批量/并发续取"未演示（后续 Marketplookup/并行度可扩展）。
- 属演示层；真正的 watchdog 定时续取仍未改（留给 roadmap 后续，风险权衡）。

## 后续建议
- 下一可借力点（按调研强度）：
  1. **把 `selfdrive_pick_next` 接进 watchdog/无人值守"按推荐续取"**（定时流水线在"无活跃任务"分支按 want 取单——本轮先用 demo 证明了取单+闭环可行；接入 watchdog 需谨慎，作为中改）；
  2. **executor 注册 + 路由**（远期 Marketplookup 完整形态）；
  3. **CI 徽章实时化**。

## 超额内容
- award_demo 从"展示工具"升级为"展示自驱闭环"，评审看一次就明白"AI 自己推荐、自己取、自己做、自己验、再推进"。

## 来源
- 前置：R26 `selfdrive_pick_next`、R23 `task_triage want`、R21 `task_triage`。
- 代码：`scripts/award_demo.py`。