# 2026-09-24 日志

## Laya 集成（可选外部决策工具）— 完成

- **目标**：下载安装 laya，作为 fist-mbt 可选工具；自动探测有无 laya，有默认用上、无则降级；评估对任务拆解边界/类型判断的价值并给出方案。
- **路线**：独立可选工具（新增 `laya_decide`），不侵入现有 schedule/omega/decompose。
- **产物**：
  - `scripts/laya_decide.py` — Python sidecar（--probe 探测 0/3/1；stdin JSON 决策 → stdout JSON）
  - `src/server/laya_js.mbt`（js-only node spawn）/ `laya_native.mbt`（native 桩）+ `server.mbt` 注册工具
  - `docs/laya.md`、`BACKLOG.md` F009
- **验证**：js 目标 build 0 errors；`moon test --target js` 135/135 全绿；端到端两条路径实测通过（可用 available:true + 降级 available:false）。
- **价值结论**：Laya 补「规则分级语义盲区」—— difficulty 定拆解粒度、domain 定派发、needs_tools/is_sensitive 做工具/敏感门控；低置信度 escalate 交现有 LLM/规则兜底。

## 项目打磨（参赛交付「拿去即用」）— 完成

- **文档对齐**：工具数 36/41 → 实际 57；测试 103 → 135；版本号统一 0.2.3（USAGE/申报书/CHANGELOG 补 0.2.3 条目）。子代理更新 README/USAGE/AGENTS/申报书。
- **绝对路径硬编码清理**：测试/smoke 的 `E:/proj/*` → `/proj/*`；修 `omega/check.mbt` 硬编码 Windows 分隔符 `"\\"`（改成分隔符跟随输入，Linux 也能拼对）。
- **跨环境重大 bug（根因）**：JS 目标 SQLite 后端依赖 `mizchi/sqlite` 的 `node:sqlite` `returnArrays`。实测 **node 23 → returnArrays 不生效→ all() 返回对象行 → mizchi 按数组行取列 → 全部列读空 → 28 测试失败**；**node 25 → returnArrays 生效 → 135/135**。即 **本项目 JS 目标需 Node ≥ 24**。已在 README/AGENTS 写死要求。
- **可复现构建**：依赖全公开（mizchi/sqlite、colmugx/mcp、moonbitlang/*），新环境 `moon update` 后即可 build；无需 vendor/登录。
- **WSL2(Linux) 验证**：moon 0.1.20260920 + node v25.2.1 + libsqlite3-dev → `moon test --target js` 135/135 全绿；node 23 → 28 失败（佐证版本约束）。
- **native 实现跨环境全绿（新的 goal）**：新版 moon 不向下游传播依赖 `cc-link-flags`，故给**全部 14 个项目 moon.pkg** 补 `options(link: {"native": {"cc-link-flags": "-lsqlite3"}})`。**WSL(Linux) `moon test --target native` → 135/135**；Windows 需 VS Build Tools（D:\VisualStudioBuildTools）+ C:\sqlite-dev（sqlite3.h/sqlite3.lib），在同一会话 `Enter-VsDevShell` + 追加 INCLUDE/LIB 后 native 亦全绿 → 两端 js+native 全绿。

## 冲刺前10：用 fist-mbt 自驱打磨自身（自我迭代=DEMO）
- **方法**：用 `selfdrive`（审视报告 → `selfdrive_publish_next` → `task_plan_deep` → 九态闭环）+ `docs/polish-plan.md` 初稿；把 5 个瓶颈 A–E 发布为自驱根任务并逐个闭环。
- **产出**：
  - `docs/polish-plan.md`（打磨计划初稿）
  - `docs/selfdrive-walkthrough.md`（端到端自驱 trace 留存）
  - README 顶部一句话卖点 + 环境要求置顶 + 一键自检 +「已知边界」节
  - `cmd/cli/main.mbt` 修复（plan 前缺 claim → 已加 claim）
  - `.github/workflows/ci.yml` 重写（native 必绿 + 补 moon update + windows js 轨道）
- **自驱轨迹**：审视报告 `memory/reviews/20260924.14.10.00.md` → publish A(T0r33)/B(T0r34)/C(T0r35)/D(T0r36)/E(T0r37) → 逐个 claim/execute/submit/verify 完成；幂等验证「发布0/跳过5」。
- **回归**：`moon test --target js` 135/135 全绿，无回归；`moon fmt` 通过。
- **发现的健壮性点**：`executions.id` 唯一导致同一任务重复 `execute` 抛 UNIQUE——README 已记录（覆盖交付物需先 reject/retry）。

### 第二轮自驱迭代（F1–F3，已交 git 基线 1950ddf）
- **F1（T0r38）**：新建演示任务 T0r41 用 `task_plan_deep` 递归拆成三层子树（T0r41.1.1/1.2/2.1/2.2），生成 `docs/task-plan-deep-walkthrough.md`，证明 selfdrive 发布 + 递归拆解组合可用。
- **F2（T0r39）**：`USAGE.md` 补"最小端到端三步"（list→publish→get）+ `E:/proj/demo` 中性化为 `/proj/demo`。
- **F3（T0r40）**：全部打磨入 git 基线 `1950ddf`（49 文件 / +1515 行），工作树干净（仅 IDE 目录未跟踪）。
- 两轮自驱闭环（A–E + F1–F3）全部走完 publish→claim→execute→submit→verify；回归 `moon test --target js` 135/135。

### 第三轮（G1–G2，一键自检）
- **G1（T0r42）**：新增 `scripts/mcp_smoke.py`——一键拉起 MCP server 并 tools/list + publish_parallel + get 断言，实测输出 `MCP-SMOKE PASS`（57 工具 / publish→T0r44 / get→待领取）；README 快速开始指向它。
- **G2（T0r43）**：USAGE 实测回写（自检脚本与三步示例一致，文档即实现）。
- 三轮自驱（A–F–G）共 10 个任务闭环；`moon test` 135/135，`python mcp_smoke.py` PASS。

### 第四轮（H1，executions 幂等化）
- 修 `insert_execution`：`INSERT` 改 `ON CONFLICT(id) DO UPDATE`——同 task+同 created_at 重复 execute 优雅覆盖，不再抛 `UNIQUE constraint failed`。
- 新增幂等单测 `src/store/executions_upsert_test.mbt`；回归 **136/136**（135→136）。
- README：badge 及测试数 135→136；「已知边界」execute 行改述为"幂等覆盖"；已 commit `c1c61d3`。

### 第五轮（tools↔文档一致性核查 + 「文档即实现」以数同步）
- **实证**：`moon test --target js` 实测 **136/136 全绿**；对照 server.mbt 实际注册 57 工具，README 57/57、USAGE 全覆盖、AGENTS 概览不冲突。
- **修复 5 处「135→136」当前态文档**：USAGE:28 快速开始、AGENTS:95 双端全绿、项目申报书 §2/§4（赛事提交）、docs/selfdrive-walkthrough:56。归档型文档（memory/reviews/reports/CHANGELOG）保留 135 为历史实录。
- **README 生命周期标题**「八件套」→「十二件套」（表格实列 12 行，与申报书 §5 模块表 12 印证）。
- 审视报告 `memory/reviews/20260924.16.30.00.md` 落盘；Next Tasks：I1（docs/features 逐个跑 AC）、I2（真实 CI，待 push 授权）。

### 第五轮续 / I1+I2p（native 轨道实跑验证 + 边界自曝）
- **实证 native 双端**：`moon test --target native` 默认并行**偶发崩溃** `0xc0000374`（堆损坏/竞态，Windows 特有）；**单独 store 7/7、`-j 1` 串行全量 136/136**；js 全量 136/136。产品单进程不受影响 → 判为测试并发工件，非产品 bug。
- 新增 **`scripts/native-env.ps1`**：一键装载 Windows native 环境（vswhere 自动探测 VS Build Tools + 探测/可覆盖 sqlite-dev，`-Run` 直跑），解决「只加 sqlite 路径不载 VS → 半装载堆崩溃」的困惑。
- README/AGENTS 「已知边界」主动自曝：Windows native 并行偶发、串行 `-j 1` 解法、native-env.ps1 指向（文档即实现）。ci.yml native 为 ubuntu，无此坑，不改动。
- 提交 `4114400`。

### 第六轮（push 同步 + 揪出并根治 CI 常年全红）
- **解锁 push**：用户要求「每轮稳定后同步（日志/报告/数据库）」。推送 11 提交到 origin/master（第 2~5 轮日志/报告/脚本/文档），并把 `fist-mbt.db`（自我迭代库：72 任务/41 执行/18 specs/11 心跳）入 gitignore 豁免入库。
- **揪出 CI 常年全红的真根因**：push 后真实 GitHub Actions 三 job 全 fail，checkout 报 `unable to create symlink README.md: File name too long`。定位：git 索引里 **README.md 被记为 mode 120000(symlink)**，但其 blob 是 8KB 全文 → runner 建符号链接目标超长（Linux PATH_MAX / Windows 路径长）。`git rm --cached` + `git add` 重置为 **100644** 后：**三轨道全绿**（js×ubuntu、native×ubuntu、js×windows）。此前远端 CI 从 0.2.3 起就是红的。
- README 加实时 CI 徽章；提交 `bd73e23`（模式修复）+ 徽章提交。

### 第七轮（调研先行示范：自进化 + 自我记忆(Hermes)）
- **「调研先行」首秀**：据 QClaw《自进化机制融入 fist-mbt》v2 + 两路并行 WebSearch，产出 `memory/research/20260924.selffit-research.md`。
- **自我记忆(Hermes 锚)**：分层(热/冷/技能)+字符上限+checkpoint 写时刻+冻结快照检索；Mem0 软遗忘 / A-Mem 关联；最小 MCP 工具 `memory_consolidate/memory_gc/memory_link`。
- **自进化可抄 3 机制**：Harness RSI(冻结模型只演外层+独立评测才写回)、EvolveR(验收通过蒸馏原则回写 DGM)、SE-Agent(打回入池精英保留)；MoonBit 生态 MoonClaw/mcp-sdk/pi-moonbit。
- **固化**：`docs/polish-plan.md` §二 方法论并入「调研先行 + checkpoint 写时刻」；§三 新增 F(自我记忆)/G(自进化融入) 路线。
- 已提交/推送 `3f47f85` 后本批（research + polish-plan + 日志）。

### 第八轮（走自驱闭环发布并实现 F/G：自我记忆 + 自进化蒸馏）
- **调研先行落地成自驱任务**：审视报告 `memory/reviews/20260924.17.30.00.md` Next Tasks 经 `selfdrive_publish_next` 幂等发布 F=T0r46 / G=T0r47 → claim → execute → submit → verify 全部「已完成」闭环。
- **F（自我记忆 Hermes 式，T0r46）**：子代理实现 `src/server/memory.mbt` + 注册 **memory_consolidate / memory_gc / memory_link** 3 工具（checkpoint 写时刻 / 上限软降权归档不硬删 / A-Mem 关联）；12 新用例。指挥官亲验 **js 148/148 + native `-j 1` 148/148**；审代码后补一处跨平台硬化（`mem_join` 无分隔符默认 `/`）。
- **G（自进化蒸馏 EvolveR 最小级，T0r47）**：实现 `evolve_distill.mbt` + 注册 **evolve_distill**（verify 通过→蒸馏 `[principle]` 原则写 DGM，复用 evolve_upsert 查重兼容）。
- **文档即实现**：README/USAGE/AGENTS 工具数 57→**61**、测试 136→**148**，4 新工具行与 health tools:61 已同步；实测双端 148/148 坐实。
- 临时驱动 `scripts/_mcp.py`（本轮自驱闭环用）已清理，不留残留。