# 2026-09-24 日志

## Laya 集成（可选外部决策工具）— 完成

- **目标**：下载安装 laya，作为 fist-mbt 可选工具；自动探测有无 laya，有默认用上、无则降级；评估对任务拆解边界/类型判断的价值并给出方案。
- **路线**：独立可选工具（新增 `laya_decide`），不侵入现有 schedule/omega/decompose。
- **产物**：
  - `scripts/laya_decide.py` — Python sidecar（--probe 探测 0/3/1；stdin JSON 决策 → stdout JSON）
  - `src/server/laya_js.mbt`（js-only node spawn）/ `laya_native.mbt`（native 桩）+ `server.mbt` 注册工具
  - `docs/laya.md`、`BACKLOG.md` F009
- **验证**：js 目标 build 0 errors；`moon test --target js` 135/135 全绿；端到端两条路径实测通过（可用 available:true + 降级 available:false）。
- **价值结论**：Laya 补「规则分级语义盲区」—— difficulty 定拆解粒度、domain 定派发、needs_tools/is_sensitive 做工具/敏感门控；低置信度 escalate 交现有 LLM/规则兜底。

## 项目打磨（参赛交付「拿去即用」）— 完成

- **文档对齐**：工具数 36/41 → 实际 57；测试 103 → 135；版本号统一 0.2.3（USAGE/申报书/CHANGELOG 补 0.2.3 条目）。子代理更新 README/USAGE/AGENTS/申报书。
- **绝对路径硬编码清理**：测试/smoke 的 `E:/proj/*` → `/proj/*`；修 `omega/check.mbt` 硬编码 Windows 分隔符 `"\\"`（改成分隔符跟随输入，Linux 也能拼对）。
- **跨环境重大 bug（根因）**：JS 目标 SQLite 后端依赖 `mizchi/sqlite` 的 `node:sqlite` `returnArrays`。实测 **node 23 → returnArrays 不生效→ all() 返回对象行 → mizchi 按数组行取列 → 全部列读空 → 28 测试失败**；**node 25 → returnArrays 生效 → 135/135**。即 **本项目 JS 目标需 Node ≥ 24**。已在 README/AGENTS 写死要求。
- **可复现构建**：依赖全公开（mizchi/sqlite、colmugx/mcp、moonbitlang/*），新环境 `moon update` 后即可 build；无需 vendor/登录。
- **WSL2(Linux) 验证**：moon 0.1.20260920 + node v25.2.1 + libsqlite3-dev → `moon test --target js` 135/135 全绿；node 23 → 28 失败（佐证版本约束）。
- **native 实现跨环境全绿（新的 goal）**：新版 moon 不向下游传播依赖 `cc-link-flags`，故给**全部 14 个项目 moon.pkg** 补 `options(link: {"native": {"cc-link-flags": "-lsqlite3"}})`。**WSL(Linux) `moon test --target native` → 135/135**；Windows 需 VS Build Tools（D:\VisualStudioBuildTools）+ C:\sqlite-dev（sqlite3.h/sqlite3.lib），在同一会话 `Enter-VsDevShell` + 追加 INCLUDE/LIB 后 native 亦全绿 → 两端 js+native 全绿。

## 冲刺前10：用 fist-mbt 自驱打磨自身（自我迭代=DEMO）
- **方法**：用 `selfdrive`（审视报告 → `selfdrive_publish_next` → `task_plan_deep` → 九态闭环）+ `docs/polish-plan.md` 初稿；把 5 个瓶颈 A–E 发布为自驱根任务并逐个闭环。
- **产出**：
  - `docs/polish-plan.md`（打磨计划初稿）
  - `docs/selfdrive-walkthrough.md`（端到端自驱 trace 留存）
  - README 顶部一句话卖点 + 环境要求置顶 + 一键自检 +「已知边界」节
  - `cmd/cli/main.mbt` 修复（plan 前缺 claim → 已加 claim）
  - `.github/workflows/ci.yml` 重写（native 必绿 + 补 moon update + windows js 轨道）
- **自驱轨迹**：审视报告 `memory/reviews/20260924.14.10.00.md` → publish A(T0r33)/B(T0r34)/C(T0r35)/D(T0r36)/E(T0r37) → 逐个 claim/execute/submit/verify 完成；幂等验证「发布0/跳过5」。
- **回归**：`moon test --target js` 135/135 全绿，无回归；`moon fmt` 通过。
- **发现的健壮性点**：`executions.id` 唯一导致同一任务重复 `execute` 抛 UNIQUE——README 已记录（覆盖交付物需先 reject/retry）。