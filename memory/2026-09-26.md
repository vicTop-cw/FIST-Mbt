# 2026-09-26 打磨收尾日志

## 阶段
用户指令：暂停新增推进，全力打磨收尾；用兄弟项目 orc-cli（实为 ocr-local）找出潜在问题并修复；不清楚 fist-mbt 是否已有「找目标项目潜问题」能力，看如何引入；工具要成 CLI＋MCP＋skill 多形态；全程用 fist-mbt 自身能力来开发。

## 已做（本轮打磨）
### 1. 深度逻辑审计（外部静态规则命中判定）
- 用 ocr-local（e:\IDEProjects\AI\ocr-local，moonbit-rules.json 20 条）scan FIST-Mbt，逐项判定：
  - store_sqlite `exec_if_ok` 的 `ignore(stmt.execute())`：仅 DDL/PRAGMA，安全
  - registry `Map.set`：可变 Map 就地生效，规则 map-set-no-rebind 假设错误
  - ability_coverage / jaccard / gate passed-total / phi sum-win：均有除零守卫
  - spec.mbt `unsafe_get`：受控循环内，安全
- 亲读核查候选点：
  - registry.mbt:197 `rows[0]`：有 `rows.is_empty()` 三元守卫，安全
  - engine.mbt:1131-1200 Kahn 拓扑：环残留按原序追加尾部 + ready 空退出，安全
- 派 general-purpose 子代理深度审计（ID 0ff2e232）：17 处高危边界抽查全部安全（MC 统计钳位 n≥10、substring end≤len、board_ascii 百分位仅 n≥3 分支、parse_simple_double divisor≥1、ops_watchdog unwrap 有 is_some 守卫等）
- **结论：ocr-local 静态规则命中全为误报/安全模式，无直接可修的崩溃缺陷**（JS 后端 307/307 为既有权威门槛）

### 2. 新增 issue_scan 工具（打磨收尾核心交付：找目标项目潜问题）
- 把 ocr-local「规则路由交给 agent 读文件」模式内建为纯 MoonBit，免外部二进制
- 10 条 MoonBit 高危规则：除零/空数组下标/无保护 unwrap/unsafe_get/无保护整除/substring 越界/ignore 丢错/字符串下标/潜在溢出/空集合单例
- 递归收集目录下 .mbt 逐行字面量匹配（不走正则，String::contains 字面量 needle）
- 产出 {scanned_files,total_findings,by_severity,by_rule,findings}，命中可直接喂 report_bug 形成"扫描→上报→修复"闭环
- 无绝对路径硬编码：相对路径 + 目录防御（拒绝空/绝对/穿越/盘符），与 bugreport 同款防御
- 文件：src/server/issue_scan.mbt（纯逻辑）+ issue_scan_test.mbt（+3 测试）+ server.mbt 注册（工具 103→104）
- mcp_smoke.py 增 issue_scan 命中校验（期望 104）

### 3. 自举演示（用 fist-mbt 自身能力编排）
- publish_parallel 发布打磨根任务（T0r260，polish ns）
- task_plan_deep 拆解成 3 层递归树（T0r260.1/.2/.3 → .N.M 叶）
- 全程 MCP 链路真实可调，工具数 104

### 4. 回归验证（全部通过）
- `moon test --target js -j 1`：316/316 全绿（终态；307 → +4 issue_scan +5 决策分支）
- `moon build --target js cmd/main`：0 errors
- `python scripts/mcp_smoke.py`：PASS（tools 104 + issue_scan 命中 2 条 + publish + get）
- `moon info && moon fmt`：.mbti 更新（server +5 行），既有文件格式化噪音保留

### 5. 文档校准（文档即实现）
- AGENTS.md：工具 103→104，运维·日志/缺陷/成本/调度 12→13 组加 issue_scan
- docs/agent-map.md：316 测试/104 工具/分组表
- docs/deliverable.md：硬指标 316/316、104 工具、新增功能轮 70（规则驱动源码扫描）

### 6. issue_scan 三形态补齐（CLI + skill）
- CLI：`scripts/issue_scan.py <dir> [--max-findings N]`（薄封装 MCP，扫描逻辑单真源，ESM shim 同 mcp_smoke），已登记 scripts/README.md 索引
- skill：`docs/issue-scan-skill.md`（何时用/怎么调/规则书/判真伪/闭环/边界），已链入 docs/agent-map.md
- 跨项目泛化验证：CLI 扫 `src/` → 105 文件 / 207 findings（by_severity 25/60/122，多数为测试文件的数组下标=安全场景，需 agent 判真伪）
- 降噪增强（同轮后续打磨）：新增 `include_tests` 参数（默认 false 只扫产品代码，跳过 `_test.mbt`/`_wbtest.mbt`）——src 扫描 105文件/207命中 → **65 文件 / 87 命中**（high 25→8、string-index 108→18），工具聚焦产品代码、可信度大增；新增 lint_4 测试（默认跳过/包含测试文件），全量 307→311

### 7. 决策分支模块（用户打磨方向：功能太多难决策 / 复杂多任务不知用哪些功能）
- 新增 `src/server/laya_route.mbt`（纯计算）：内建机制族（拆解调度/深度验证/预算成本/可靠性容错/排程路由/目标校验/代码质量扫描/自进化），按任务描述关键词子串打分选最匹配 `feature_route` + 复杂度启发式给 `split_n`，`source=fallback` 确定性可复现。
- 扩展 `laya_decide` 工具（无新工具，避免计数 churn）：Laya 可用→sidecar 决策（难度/机制）；无 Laya→本模块产出结构化决策（available=false + decision:feature_route/split_n/reasons）。
- 依据：用户要求「当有 laya 让它对功能做决策；无 laya 配自包含决策模块做分支」——layla_route 即无 laya 时的确定性分支。
- 实测边界：本机 laya 可 import 但 predict 卡（全调用无内部超时，300s），故 `laya_auto` **保持默认 false**（避免每次 plan_deep 卡动现网）；laya 驱动深度走显式 `laya_auto=true` 或 `laya_decide`（README/AGENTS 已注明）。**研发方向 ①「递归层数默认让 laya 推断」受 predict 无超时边界约束，按需开启。**
- 测试：laya_route_test +5（选族/基线 split/复杂度/确定性），全量 311→316。

### 8. issue_scan 挂进无人值守
- `templates/cron_pipeline_meta_prompt.md`：工具表加 `issue_scan`/`report_bug`；分支④（生成下轮提示词前）注入「先调用 issue_scan 扫源码找高危命中 → 真缺陷 report_bug(publish_task=true) 入账自动发布修复根任务」→ 找潜问题成为无人值守常态（误报不回填防刷账）。
- 文档同步：测试 311→316 全量校准（README/README_EN/AGENTS/deliverable/scoring_rubric/BACKLOG/快照）。

### 9. 第 30 次门禁实测（外部 provider 限流阻塞，非项目回退）
- 跑 `score_gate.py --evidence _snapshot.md --ai1-json {...}`（第 30 次评估，目的：验证 R112 打磨是否推高概率）。
- 结果：**AI1 pass（0.74/0.89/0.98）· AI3 pass（0.75/0.88/0.97）**；**AI2(LongCat)/AI4(Kimi) 返回空输出**（rc=0 无 SCORE_JSON）→ 门禁 PASS=否。
- 诊断：`atomcode ... --provider longcat` 直接跑输出 `[rate-limited] 5h window exhausted — resets around 12:11`——**外部 provider 配额耗尽**，非概率回退、非瞬态抖动（AI3 默认模型可用即证）。
- 处置：不暴力重试（外部配额阻塞）；快照 §六 + 本日志如实记录；待配额窗口重置（约 12:11）后重跑 AI2/AI4 补全第 30 次评估。
- 结论：R112 打磨体本身完整全绿（104/316）；门禁测量被外部配额推迟，非失败。

### 10. Agent Contract 7 字段注入自驱任务（five-directions P1）
- 新增 `sd_agent_contract(task)` 纯函数（src/ops/ops_selfdrive.mbt）：确定性生成 Objective/Constraints/Tool policy/Stop conditions/Escalation/State discipline/Evidence 七段执行契约。
- 接入 `selfdrive_publish_next`：每个自驱发布根任务描述追加该契约（幂等标记[review:...]保留），强化「会自我迭代的 AI 项目管理工具」自驱执行纪律——不改生命周期语义、零回归。
- 测试：ops_selfdrive_test +1（contract 7 字段含全标记+Objective 回填）；publish 测试增强断言契约已注入；全量 316→317。
- 文档：README/EN/AGENTS/deliverable/scoring_rubric/申报书/ARCHITECTURE/快照 测试 316→317 + contract 说明。

### 11. 调研先行：产出物验证（Did it work?）设计（下一候选机制）
- 调研 5 来源合成为 `memory/research/20260926.output-artifact-validation.md`：核心原则「报告不是证据，只有交付产物实际状态才证明工作发生」（dev.to/arXiv 2607.02615/AWS 三层/AWS pass^k/tamizuddin）。
- 设计 `output_validate`：五层证据梯（示意→动作→测试→产物状态→外部接受），确定性硬门 + 产物 invariant 门 + 可选 LLM judge + enforce（fail→report_bug/reject）。
- 复用 `run_check`/`issue_scan`/`omega gate`/`eval_feedback`/`report_bug`（拿来主义），不重造 exec/解析轮子；验收 ≥5 测试。
- 这是下一功能轮候选，不在此轮实现（当前 R112 处于门控维持态，避免 churn）；设计就绪待实现。

## 待办/遗留
- skill 形态为站内文档（docs/issue-scan-skill.md），未打包进外部 agent 技能注册系统（.codeartsdoer/skills 为空目录）——可后续用 skill-creator 固化
- issue_scan 为字面量匹配，有误报/漏报，命中需 agent 判真伪
- temp/ 测试落盘由 cleanup_artifacts.py --check 作干净度门禁（既有）
- 输出用中文，测试用 test 块，guard/match 冒号后换行（均已遵循）

## 来源
- 兄弟项目 ocr-local 规则：e:\IDEProjects\AI\ocr-local\rules\moonbit-rules.json（20 条）
- 本项目：src/server/issue_scan.mbt、src/server/server.mbt、scripts/mcp_smoke.py、AGENTS.md、docs/agent-map.md、docs/deliverable.md
