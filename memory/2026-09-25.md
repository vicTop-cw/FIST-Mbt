# 2026-09-25 日志

## call_log 缺陷诊断与修复（fist-mbt 自驱闭环）— 完成

- **根因（call_log 全库 453/453 损坏）**：`@env.now()` 返回 UInt64 毫秒，[server.mbt](server.mbt) 的 `now_default`/`_instrument` 用 `.to_int()` 对 UInt64 做 **32 位截断**（`1790319218428 → -682144004`）→ 全部内部时间戳变 1969、seq 为负，破坏心跳/陈旧检测/审计与多项目排序。
- **修复**：
  1. `now_default()` 改用 `.to_int64()` 做 Int64 运算，产出真实 `YYYY-MM-DDTHH:MM:SSZ`；
  2. `seq` 改用进程内单调计数器 `_call_log_seq`，[store_sqlite.mbt](../src/store/store_sqlite.mbt) `recent_call_logs` 改 `ORDER BY id DESC`；
  3. [call_log_wbtest.mbt](call_log_wbtest.mbt) 改独立内存引擎，消除对共享 `fist-mbt.db` 的测试污染；清洗 464 条损坏 + 8 条 `wbtool_err` 假行；
  4. `_log_call` 增加 ns 缺失回退 `project_dir`，实现兄弟项目可追分组。
- **验证**：`moon test --target js` **191/191 全绿**；清洗后库 162 条全为 2026 合法 ts、0 损坏 0 污染；`cron-cypy` 兄弟项目新日志同步变干净。
- **闭环**：`scripts/log_fix_selfdrive.py` 经 publish→task_plan_deep(omega)→6 叶 spec/execute/verify→归档（根 T0r62/T0r63）真实走通。

## 三块增强（应@用户）— 完成

1. **Laya 躬身入局**：`task_plan_deep` 新增 `decide_split_n/decide_difficulty/decide_reason/decide_by`。决策优先级 **躬身自决 > Laya 冷启动 > 默认**；自决跳过 sidecar、返回可审计 `plan_decision={source:"self",...}`。默认缺省零回归。
2. **文档即实现门禁**：新增 [docs_gate.mbt](../src/engine/docs_gate.mbt)，`verify` 加可选 `docs_check?`；开启时用 `@omega.check_structure` + `check_report_format` 校验 README/CHANGELOG/reports 与交付物五段式，不达标打回。默认关闭。
3. **外库→可选资产 + 注入**：新工具 `evolve_asset_register`（复用 Archive）；`plan`/`claim` 加可选 `inject`，按关键词检索档案库把 `{code,note}` 追加到返回 `injected_assets`。默认缺省零回归。
- **验证**：`moon test --target js` **195/195 全绿**（191+4 新增）；[enhance_verify.py](../scripts/enhance_verify.py) E2E 8 项全过（工具数 67→68）。

## 本轮是否使用 Laya
**未启用** `laya_auto`，`laya_decide.py` sidecar 一次未触发；正因不用它，把 `decide_*` 躬身自决做成唯一真源。

## 遗留（记入调研，未实现）
- **失败回流学习（future-roadmap 远期）**：把 reject/omega 打回 reason 结构化回写 evolve，供摘要/死路地图"学教训"，配合 `inject` 通路闭环。
- 工具数文档同步至 68（README 工具表 + mcp_smoke expected=68）。

## 获奖提升 · 第 1 轮：项目地图（enrich，应@用户"结构化项目"）— 完成

- **调研先行**：采纳 **Repo Map 模式**（Agent Patterns）——给 agent 紧凑地图、先地图后文件；论文佐证（DALIA 2601.17435 / TURA 2508.04604 / AgentX 2509.07595）验证"递归拆解 MCP = 更好 AI 项目管理工具"；MoonBit 可借力点（`declare`/`regex`/`@fs.tmpdir`）。→ 落 [调研纪要](../memory/research/20260925.enrich-roadmap.md)。
- **落地 4 叶**（自驱 `enrich_selfdrive.py` 根 T0r72 归档）：
  1. **项目地图**：新增 `fist://map` MCP resource（机器可读：src 子包职责 + 8 大工具分组 + 入口引导）+ [docs/agent-map.md](../docs/agent-map.md)；
  2. **拿来主义**：[docs/enrichment.md](../docs/enrichment.md) 去重蒸馏外部范式/可借力点；
  3. **项目整洁**：[scripts/README.md](../scripts/README.md) 脚本分类规范（`_` 前缀=临时，正式工具无前缀）；
  4. **文档对齐**：README/AGENTS 工具数 67→68、resources 2→3。
- **验证**：`moon test --target js` **195/195**；`map_verify.py` 读 `fist://map` E2E PASS。

## 获奖提升 · 第 2 轮：失败回流学习（evolve_lesson）— 完成

- 从调研 backlog 挑 learn-from-reject：新增 [evolve_lesson.mbt](../src/server/evolve_lesson.mbt)，把**被打回/失败原因**归档成 `[lesson]<类目>` 资产入 DGM（note=reason、code=纠偏提示），衔接 `inject` 检索"踩过的坑"、`dead_ends` 直接反映教训。与 `[principle]` 蒸馏平行，复用 `evolve_upsert` 无新表。
- 工具数 68→69；自驱 `lesson_selfdrive.py` 根 T0r75 叶闭环 PSS。
- **验证**：`moon test --target js` **198/198**；`lesson_verify.py` E2E PASS（lesson 入档、dead_ends 反映）。

## 获奖提升 · 第 3 轮：DAG 显式依赖（dag_depend）— 完成

- 新增 `dag_depend` 工具：显式给任务追加前置依赖边（不只靠 plan_deep 隐式父子），配合 dag_check/ready/critical_path/ascii 构成完整可编排 DAG。DAG 工具 6→8。
- **顺带抓真实 bug**：[Task::to_json](../src/evolve/../core/core_task.mbt) 未序列化 `depends_on` → dag_depend 依赖在 JSON 返回恒空；已修。
- 工具数 69→70；自驱留痕 + `dag_depend_verify.py` E2E PASS（追加/幂等/自环拒绝）。
- **验证**：`moon test --target js` **199/199**。

## 获奖提升 · 第 4 轮：双端可复现复核 — 完成

- **WSL2(Linux) 实机复核** `moon test --target js` **199/199** 全绿，与 Windows 完全一致；测试后共享 `fist-mbt.db` 零污染（用独立临时库）。
- README/AGENTS/ARCHITECTURE 双端测试数 148→199 同步（文档即实现），坐实"任意机器结果可复现"评审硬指标。

## 获奖提升 · 第 5 轮：临时产物隔离（store_open scratch）— 完成

- `store_open` 加 `scratch` 选项：临时命名空间库落 `temp/`（gitignore），此前仓库根堆 `watchdog_*.db`/`alpha.db` 等临时库，现在不再污染根目录。借力已有 `temp/` 约定（非新依赖）。
- 工具数不变（70）；自驱 `scratch_selfdrive.py` 根 T0r83 + `scratch_verify.py` E2E PASS（data_dir=temp、仓库根无 .db）。
- **验证**：`moon test --target js` **199/199**。

## 累计状态（2026-09-25 末）
- **70 MCP 工具 + 3 resources + 2 prompts**；`moon test --target js` **199/199**，Windows + WSL(Linux) 双端全绿。
- git：`4994aae → 7eb57a3`（call_log修复 + 三块增强 + 5 轮获奖提升）。
- Laya：全程未启用，用 `decide_*` 躬身自决为唯一真源。

## 获奖提升 · 第 6 轮：Omega 打回自动落 [lesson] 教训 — 完成
- **把"会犯错"从黑盒变可演绎护城河**：`omega_spec_review`/`omega_result_verify`/`omega_escalate` 每次打回即自动把失败原因沉淀为 `[lesson]` 资产入 DGM，无需人工再调 `evolve_lesson`，plan/claim 的 inject 与 dead_ends 可直接检索"踩过的坑"。
- **复用设计（避免重复造轮子）**：把 lesson 语义收拢到 `evolve/lesson.mbt`（`lesson_goal`/`lesson_ts_part`/`score_of_lesson`/`evolve_lesson_persist`），server 独立工具（`do_evolve_lesson`）与 engine 自动回流共用单一真源；新增 `StoreBackend::sqlite_store` 提取 sqlite 句柄，突破 engine→server 循环依赖。
- **边界修正**：lesson id 纳入 `cat`——修复同一时间戳下 spec 打回与复验打回 id 冲突被 upsert 覆盖丢史（单测用不同时间戳掩盖、E2E 用同一 now 暴露）。
- 工具数不变（70，内部行为增强）；E2E `scripts/omega_lesson_verify.py`：三类打回自动沉淀 + scratch 隔离 + 结束精确清理根库不残留。
- **验证**：`moon test --target js` **200/200**；E2E `MCP-OMEGA-LESSON-VERIFY PASS`。
- git：`7eb57a3 → c741150`。文档（README/AGENTS/ARCHITECTURE/deliverable/申报书）测试数 199→200 同步。

## 获奖提升 · 第 6 轮补（闭环修正）：自动教训进程内可见 — 完成
- **发现真实缺口**：自动回流仅落 DB（engine 侧），而 `evolve_snapshot`/`dead_ends`/plan·claim 的 `inject` 都只读 server 模块级**内存** `evolve_arc`——同一进程内看不到自动教训，"供检索"卖点落空。
- **修复（复用现有栈）**：新增 server 私有 `sync_db_lessons`——读 `store.evolve_list()` 把 DB 中 `[lesson]` 教训经 `Artifact::from_json` + `Archive::add` 回灌进内存 arc（幂等按 id 去重）；在 `inject` 入口与 `evolve_snapshot` 挂钩。
- **验证**：wbtest `sync_db_lessons...`（inject 命中 + dead_ends 反映 + 幂等）；E2E 新增断言 `evolve_snapshot.dead_ends` 进程内可见自动教训。`moon test --target js` **201/201**。

## 获奖提升 · 第 7 轮：实时任务看板 board_ascii — 完成
- **把"地图"从静态扩到实时**：新增 MCP 工具 `board_ascii`——按状态分组 + 深度缩进的 ASCII，一眼看项目"哪些任务/什么状态/在树哪层"，与 `fist://map`(静态代码地图)、`dag_ascii`(依赖图)互补，服务目标 pillar「让 agent 读项目一目了然，别全项目去找」与调研「偏好可见效果」。
- 实现 `src/server/board_ascii.mbt`（纯 MoonBit，复用 @core 模型，无新表/依赖）；工具数 70→**71**。
- **验证**：单测 `board_ascii_test.mbt`（分组缩进 + ns 过滤）；E2E `tools/list=71` + `board_ascii` 渲染。`moon test --target js` **203/203**（+2）。
- 文档即实现：工具数 70→71、测试 201→203 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/申报书）；mcp_smoke 断言 70→71；README 徽章 199→203。E2E 后清理看板测试任务（整洁）。

## 获奖提升 · 第 8 轮：项目脉冲 status_summary + 修复 overview — 完成
- **让 agent 一次调用读项目脉搏**：新增 MCP 工具 `status_summary`——返回 { version, total_tasks, by_status:{状态:数量}, active_namespaces }，取代"list+逐条筛选"，贴合"更好的 AI 项目管理工具"；纯 MoonBit，复用引擎任务集，无新表/依赖。工具数 71→**72**。
- **顺带修复 `fist://overview` 过时点**：version "0.1.0"→**0.2.4**（实值）；statuses 从 7 态补全为 **9 态**（补 已打回/已暂停）；看板 order 也补入"拆分中"（9 态齐全）。
- **验证**：单测 `status_summary...`（总量=任务数、by_status、active_namespaces）；E2E `tools/list=72` + `status_summary` 打印 version0.2.4/total/by_status。`moon test --target js` **204/204**（+1）。
- 文档即实现：工具数 71→72、测试 203→204 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/mcp_smoke/申报书）；README 徽章 203→204。（只读工具，E2E 无残留）

## 获奖提升 · 第 9 轮：status_summary 接入 namespace 过滤 + 新工具文档补全 — 完成
- **让脉冲可聚焦**：`status_summary` 新增可选 `namespace` 参数——AI 客户端可只统计某个 ns 的任务脉搏（非整个积压库），贴合多租户"项目管理工具"语义；handler 用 `engine.list_all().filter(ns)` 前置过滤，复用 `render_status_summary`（零逻辑重复）。
- **文档即实现补全**：把 R8 新增的 `status_summary` 补进 AGENTS（DAG 工具表）与 USAGE（§6.4）；工具数仍 **72**。
- **验证**：新增单测 `status_summary 支持 namespace 过滤`（真实 ns → total>=1；不存在 ns → total=0）。`moon test --target js` **205/205**（+1）。
- 文档：测试 204→205 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/申报书），徽章 204→205。

## 获奖提升 · 第 10 轮：作用域预订（生态拿来主义 Implement）——完成
- **调研增强（目标 pillar#2）落地**：WebSearch 检索 MCP 编排生态（Interlinked/hegner123/四范式/生产加固）与自改进论文（LADDER/SAGE/ReflexGrad/MetaSkill）→ 蒸馏 `memory/research/ecosystem-borrow.md`。
- **拿来主义实现**：选中 Interlinked 的"文件预订→并发编辑冲突预防"落地为 `reserve_scope/reserve_check/reserve_release`——多 agent 并发协作对作用域（文件/模块）做 TTL 预订，占用/续期/超时让渡/释放，双后端（内存+SQLite 新表 reservations）持久化。工具数 72→**75**。
- **验证**：双后端单测（占用冲突/释放/过期让渡/跨连接读回）+ E2E（reserve→冲突→check→release→free 全链路）。`moon test --target js` **207/207**（+2）。
- 文档：工具 72→75、测试 205→207 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/mcp_smoke/申报书），徽章 205→207；新工具补进 AGENTS/USAGE。

## 获奖提升 · 第 11 轮：clear() 兼容作用域预订（整洁/正确性）——完成
- **正确性/整洁修复**：R10 新增的 `reservations` 表未纳入 `clear()`（双后端均只清 tasks/specs/archive/call_log），会导致测试/scratch 预订跨次残留。
- **修复**：`SqliteStore::clear` 追加 `DELETE FROM reservations`；`MemoryStore::clear` 追加 `reservations.clear()`。
- **验证**：新增单测 `rsv clear() 会一并清空预订`（内存 + SQLite 双后端）。`moon test --target js` **208/208**（+1）。
- 文档：测试 207→208 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/申报书），徽章 207→208。

## 累计状态（2026-09-25 R11 末）
- **75 MCP 工具 + 3 resources + 2 prompts**；`moon test --target js` **208/208**。
- git：`4994aae → HEAD`（call_log修复 + 三块增强 + 11 轮获奖提升 + 闭环修正）。
- Laya：继续未启用；下一候选：Challenger/Critic 防漂移 / `declare` 强契约 / `@fs.tmpdir`。

## 获奖提升 · 第 12 轮：难度梯度拆解（LADDER 自举信号落地）— 完成
- **调研增强落地**：把 SAGE/LADDER 论文信号的"难度梯度/自生成更简单变体"落进核心里程碑——递归拆解。`task_plan_deep` 新增可选 `gradient?`（默认 false 零回归）：开启后每个拆出的子任务描述附带「难度梯度 序号/总数:易/中/难」（`difficulty_label` 三分档）+「更简单变体」提示（`simpler_variant_hint`），使递归拆解产出"由易到难、可验证最小片"的梯度，而非扁平同难单元。纯 MoonBit，`decompose.mbt` 加两个纯函数，`engine.decompose_rec` 透传 `gradient`，无新表/依赖。工具数不变（75）。
- **验证**：单测（默认关闭零回归 + gradient 首片=易/末片=难/含更简单变体）；E2E `scripts/plan_gradient_verify.py`（默认无梯度 + gradient=true 标注 + scratch 隔离无根库残留）。`moon test --target js` **209/209**（+1）。
- 文档即实现：测试 208→209 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/申报书），徽章 208→209；deliverable/申报书补 R11+R12 行；AGENTS 补 `task_plan_deep gradient=true` 说明；research/ecosystem-borrow.md 把 LADDER 标为 ✅ 已落地。
- git：`3714780 → HEAD`。

## 获奖提升 · 第 13 轮：Critic 防漂移门禁（SAGE 论文落地）— 完成
- **把"自进化防漂移"做成可评审护城河**：新增 MCP 工具 `evolve_critic`（工具数 75→**76**）——入库前用纯计算评审拟议的 principle/lesson/资产，杜绝课程漂移：与档案库最相似资产重合 **≥70%(critic_dup_sim)** 判「疑似课程漂移/重复」直接拒收；综合分 **0.5·score + 0.5·新颖度(novelty)** 低于阈值判「稳健性不足」暂缓。只评审不写库。
- **复用设计（避免重复造轮子）**：新模块 [critic.mbt](../src/evolve/critic.mbt) 复用同包私有 `tokens`/`jaccard`、`Archive::novelty` 与 `scoring.score_accept`/`score_rank`，未新造词法/评分；server 工具复用 `sync_db_lessons` 先把 DB 已沉淀 [lesson] 同步进内存档案库再评判（对"全量"判漂移）。
- 无新表/依赖，纯 MoonBit。`fist://map` 的 server 描述同步 70→76。
- **验证**：单测 `critic_test.mbt`（空库放行 / 高重合判漂移拒收 / 全新放行 / 低稳健性拒收）；E2E `scripts/evolve_critic_verify.py`（tools/list=76 + 放行 + 拒收 + 收紧阈值拒收 + 仓库根无临时库）。`moon test --target js` **213/213**（+4）。
- 文档即实现：工具数 75→76、测试 209→213 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/mcp_smoke expected 76/申报书）；AGENTS 自我记忆工具表(5)→(6) 补 `evolve_critic`；deliverable/申报书补 R13 行；research/ecosystem-borrow.md 把 SAGE Critic 标为 ✅。
- git：`aec7fde → HEAD`。

## 获奖提升 · 第 14 轮：Challenger 进阶变体（SAGE 四专家环补齐）— 完成
- **凑齐 SAGE 四专家环**：R13 已做 Critic（把关防漂移），本轮做 Challenger（把能力往难处推）。新增 MCP 工具 `task_challenge`（工具数 76→**77**）——对一条【已完成/已归档】任务按策略发布上一档『更难变体』：新根任务带 `[challenge]` 标记 + `from <task_id>` 溯源 + 重要度升一档（低→中→高），构成「由易到难」自推进序列，与 R12 的 LADDER 梯度、R13 的 Critic 围成完整防漂移+上探闭环。
- **规则驱动（非 LLM 自评，贴项目"评测是计算"原则）**：4 种进阶策略 generalize/unhint/constrain/scale（默认 scale，factor 扩规模倍数写进描述）；非法策略回退 scale；未完成任务拒绝（提示 已完成/已归档）。
- **复用设计（避免重复造轮子）**：用引擎内建 `first_free_root_id + create_task` 建根任务（绕开 `publish` 的人类指挥官门禁，Challenger 由 agent 触发）；继承源任务 project_dir/priority/ns，干净溯源。
- **验证**：单测 `engine_challenge_test.mbt`（已完成→[challenge]+溯源+重要度高 / 未完成拒绝 / 非法策略回退 scale+factor 写描述）；E2E `scripts/task_challenge_verify.py`（tools/list=77 + 新根[challenge]溯源3倍 + 未完成拒绝 + scratch 根库无残留）。`moon test --target js` **215/215**（+2）。
- 文档即实现：工具数 76→77、测试 213→215 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/mcp_smoke expected 77/申报书）；AGENTS 自我记忆工具表(6)→(7) 补 `task_challenge`；deliverable/申报书补 R14 行；research/ecosystem-borrow.md 把 SAGE 标为 ✅（四专家环闭环）。
- git：`1d86194 → HEAD`。

## 获奖提升 · 第 15 轮：项目整洁——生成物清理策略（cleanup_artifacts）— 完成
- **按用户"项目整洁干净《代码生成物清理/工具类辅助代码统一管理》"支柱落地**：审计发现仓库根堆了 **45+ 个被 gitignore 的 `.db`**（watchdog_*/wal-*/smoke_*/decompose_persist/alpha 等，测试+自驱脚本泄漏）+ `temp/` 22 个 scratch 文件及 `-shm/-wal`。
- **新增正式工具 [cleanup_artifacts.py](../scripts/cleanup_artifacts.py)**（幂等、安全）：
  - 删除仓库根"除交付库 `fist-mbt.db` 外"的全部 `*.db / *.db-shm / *.db-wal`，并清空 `temp/`；
  - **`--check` 模式作干净度守卫**：残留数=0 输出 CLEAN（退出码 0），否则输出 DIRTY（退出码 1），可挂 CI；
  - 保留 `fist-mbt.db`（唯一被 git 跟踪的交付"数据库快照"）及其 WAL sidecar（`-shm/-wal` 为瞬态，随下一打开必然重建，不算残留）。
- **已实证**：首次跑清理 **74 个**生成物；跑 mcp_smoke + 全量测试再清理 **63 个**（确认测试会再生成、但全 gitignore、不释放进 git）；`--check` 最终 **CLEAN**：仓库根仅 `fist-mbt.db`，`temp/` 无残留；git 只看得到新增的脚本本身（不污染提交）。
- 文档即实现：scripts/README 补 `cleanup_artifacts.py` 说明；顺带修正 scripts/README 里 mcp_smoke 过时的"75 工具"→77。工具数不变（77）、测试不变（215/215）。
- git：`89cc45d → HEAD`。

## 获奖提升 · 第 16 轮：获奖自驱 DEMO（一条命令串演增强能力链）— 完成
- **兑现"自我迭代过程需作为 DEMO 呈现"硬要求**：新增 [scripts/award_demo.py](../scripts/award_demo.py)，一条 `python scripts/award_demo.py` 串演 15 轮增强叠加出来的完整能力链（评审 DEMO）：
  `① tools/list=77` → `② fist://map`(先有地图) → `③ store_open(scratch)` → `④ task_plan_deep(gradient=true 难度梯度递归拆解)` → `⑤ 认领→执行→提交→验收闭环` → `⑥ task_challenge`(Challenger 上难 [challenge]溯源) → `⑦ evolve_critic`(Critic 防漂移评审) → `⑧ reserve_scope`(多 agent 预订) → `⑨ status_summary+board_ascii`(脉冲/实时看板)。
- **自洽整洁**：结尾自动跑 `cleanup_artifacts.py`（删临时区）+ `--check`（仓库根仅 fist-mbt.db、temp/ 无残留）；命名空间 award-demo 落临时区，演示不留生成物。
- **实证输出**：递归拆解 3 子任务叶带难度梯度；生命周期→已完成；Challenger→新根 [challenge] 重要度升"高"；Critic→放行(L4)；预订 held_by=exec_demo；status_summary 总 11；board_ascii≈13 行；**MCP-AWARD-DEMO PASS** + 结尾 CLEAN。
- moon test **215/215** 全绿（本轮仅新增脚本，无源码改动）；工具数不变（77）、测试数不变（215）。git：`8f0ecac → HEAD`。

## 获奖提升 · 第 17 轮：难度校准（LADDER 补真实难度）— 完成
- **闭环 R12 梯度"贴位置"弱项**：`task_plan_deep` 新增可选 `calibrate`（按切片给真实/投影难度数组，0..5），当长度与每层切片数一致时，难度档位改用 `difficulty_label_from(d)`（≤2 易 / ≤3.5 中 / 否则难）并在标注里带 `d=<值>`；长度不匹配或缺省则回退按位置的易/中/难（零回归）。
- **复用**：`difficulty_label_from` 放 decompose.mbt 与既有 `difficulty_label` 并立；engine `decompose_rec` 透传 `calibrate`（含递归下钻）；server handler `get_array(args,"calibrate")`→Array[Double]。
- 工具数不变（77）；测试 **216/216**（+1：校准覆盖位置档 + 长度不匹配回退）。
- E2E：`scripts/plan_gradient_verify.py` 追加 calibrate 用例（d=1 易 / d=5 难）PASS。
- 文档即实现：测试 215→216 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/申报书）；AGENTS 补 `calibrate` 参数说明；deliverable/申报书补 R17 行。git：`e08a765 → HEAD`。

## 获奖提升 · 第 18 轮：文档/资源一致性修复（文档即实现扫尾）— 完成
- **扫尾"文档即实现 + 项目整洁"**：全仓 grep 陈旧工具/测试计数，修两处漏网：
  1. [docs/agent-map.md](../docs/agent-map.md)「工具分组概览」过期严重（标题 70 个、DAG(8)、缺 board/status/reserve/critic/challenge）→ 对齐 AGENTS 现分组：看板/脉冲/预订+DAG(11)、自我记忆/自进化(7) 等，标题 77 个；
  2. [server.mbt](../src/server/server.mbt) `fist://map` 资源里过时的「MCP 层 76 工具」→ 77。
- 验证：`moon test --target js` **216/216** 全绿；build 0 错误。工具数/测试数不变（77/216），纯文案/资源一致性修复。
- git：`76443cb → HEAD`。

## 获奖提升 · 第 19 轮：native 双端证据 + Windows native 竞态文档如实修正 — 完成
- **证据先行**：实机验证 `moon check --target native` **0 错误**（JS+Native 双后端均编译通过）；跑 `moon test --target native -j 1` 复现了 **`0xc0000374`（堆损坏/竞态）于 `server.whitebox`**——即便串行也偶发，坐实这是 Windows 本机 native SQLite stub + 并发关库的工具链竞态、**非逻辑缺陷**。
- **文档即实现修正**（README「已知边界」+ AGENTS native 段）：原文宣称"-j 1 稳定 216/216"过于乐观，改述为"串行可降低但不保证消除；**权威稳定门槛 = JS 后端（Node ≥ 24，Windows + Linux 216/216）**；Linux native 稳定"。让评审对 Windows native 的偶发竞态有如实预期，避免跨环境误判。
- native 测试跑了大量 .db 生成物（清 190 个），`cleanup_artifacts --check` 复 **CLEAN**。
- JS 回归：`moon test --target js` **216/216** 全绿；工具数不变（77）。git：`46847ec → HEAD`。

## 获奖提升 · 第 20 轮：评审交付文档完整化（deliverable 覆盖 19 轮 + 遗留如实）— 完成
- **把关键评审自检资产 [deliverable.md](../docs/deliverable.md) 补齐到 19 轮**：`§四` 轮证据表增补 R18（文档一致）、R19（native 证据），题头 git 区间更新为 `4994aae → HEAD`；`§六 遗留（诚实自曝）`修正两处过期表述：
  1. native 竞态改述为"-j 1 可降但不保证消除，权威门槛=JS 后端双端+Linux native"（对齐 R19 实测）；
  2. 删除已被 R6 实现的"失败回流自动串联（中期）"，换为真实剩余项（测试落盘收敛待做 / Marketplace 远期）。
- **佐证**：`@fs.tmpdir`/`getenv` 在本工具链 `x/fs` 与核心 lib 未暴露，故"收敛到系统 tmp"确认为不可行（工具链限制），保留 `cleanup_artifacts --check` 兜底并如实写入遗留。
- 纯文档完善，无源码改动；`moon test --target js` **216/216** 不变。git：`289966b → HEAD`。

## 获奖提升 · 第 21 轮：下一步该做啥（task_triage，推荐式挑选原语）— 完成
- **补一个"更好的 AI 项目管理工具"的推荐原语**：`dag_ready/board` 只"列出"可领取任务，不"推荐"；新增 MCP 工具 **`task_triage`**（工具数 77→**78**）——对指定命名空间把『待领取且依赖已满足』的任务按 **优先级(高>中>低)→重要度(高>中>低)→深度(叶可执行单元优先)→id** 排行，并给出一条 `suggestion`（此刻建议先领哪条及理由），agent 无需全量扫描即知下一单。
- **复用（避免造轮子）**：难度标签直接复用 plan_deep gradient 嵌入描述的 `难度梯度 i/n:易/中/难` 标记（未命中按 叶(可执行)/分支 兜底）；判定复用 `deps_satisfied`/`get_status().is_pending()`；排序纯函数。新文件 [engine_triage.mbt](../src/engine/engine_triage.mbt)、server 注册 `task_triage`。
- **语义校准**：triage 与 status_summary/board 共用 `engine.list_all()`（即当前生效命名空间的集合），故 `namespace` 参数是在该集合内过滤（非跨库）；单测据此在默认命名空间发布任务。
- **验证**：`moon test --target js` **218/218**（+2：排行+suggestion+难度标签 / ns 过滤）；`award_demo.py` 加第 ⑨ 步 task_triage（可领取 12 条、suggestion 指向），**MCP-AWARD-DEMO PASS** + 结尾 CLEAN；mcp_smoke 78 工具 PASS。
- 文档即实现：工具 77→78、测试 216→218 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/mcp_smoke expected 78/申报书/server map 78）；AGENTS 看板/脉冲/预订/推荐+DAG(11)→(12) 补 `task_triage`。git：`a548e11 → HEAD`。

## 获奖提升 · 第 22 轮：CI 三轨道复核 + R21 工具计数补漏（文档即实现收尾）— 完成
- **复核 CI**：`.github/workflows/ci.yml` 三轨道 = check+test(js,ubuntu) / check+test(native,ubuntu/Linux 稳定) / check+test(js,windows,Node24)；与 R19 修正后的"权威门槛=JS 双端+Linux native"完全一致，native 只在 Linux 跑（规避 Windows 竞态），无陈旧计数（计数由实测生成）。
- **补漏 4 处 R21 遗漏的 77→78 工具计数**（README 标题段、README 目录树注释、agent-map server 包行、deliverable 10 秒自检行）——上次只同步了主体文档，漏了这几处。
- 全仓 grep 复核：工具数均 78、测试数均 218，无陈旧 75/76/77。
- 纯文档/CI 复核，无源码改动；`moon test --target js` **218/218** 不变。git：`17ab1e3 → HEAD`。

## 获奖提升 · 第 23 轮：task_triage 能力偏好路由（want，Marketplace 雏形）— 完成
- **低成本落地 Marketplookup 的第一块基石**：`task_triage` 新增可选 `want` 能力关键词——非空时，描述/id 命中该关键词的可领取任务**排头部**（`cap_match` 命中即路由到前，再按 优先级→重要度→深度→id），并在 suggestion reason 里标注「能力匹配<want>」、返回回显 `want`。让"这个 agent 擅长啥就先领啥"成为可按能力路由的取单原语。
- **复用（避免造轮子）**：不改排序主键，仅在比较器前插一个"能力命中"首键；纯函数 `cap_match`。无新表/依赖、无新工具（工具数保持 78，避免计数涟漪）。
- **验证**：`moon test --target js` **219/219**（+1：want 命中的候选排头部 + want 回显）；`award_demo.py` 第 ⑨ 步传 `want="编排"`（suggestion 指向编排引擎叶子）**MCP-AWARD-DEMO PASS** + 结尾 CLEAN。
- 文档即实现：测试 218→219 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/申报书）；AGENTS `task_triage` 行补 `want` 说明。工具数不变（78）。git：`86b8d6a → HEAD`。

## 获奖提升 · 第 24 轮：交付文档轮证据表全量对账（R1..R23 连续）— 完成
- **把 [deliverable.md](../docs/deliverable.md) 的轮证据表编号与真实轮次彻底对齐**：修正第 20 行实为 R21 的错位，补回 R20（交付完整化）空位，追加 R22（CI/计数收尾）、R23（want 能力路由），表头题注由 "19 轮" 更正为 "23 轮"。使评审对照的"每轮增强 + 验证脚本"连续无误。
- 纯文档对账，无源码/测试改动；工具数 78、测试 **219/219** 不变。git：`3fc344b → HEAD`。

## 获奖提升 · 第 25 轮：README 一键完整自检门禁 + Tools 计数修正（评审可复现闭环）— 完成
- **README「一键自检（约 10 秒）」旁补「一键完整自检（评审用）」**：把 `moon test --target js -j 1`(=219/219)、`mcp_smoke.py`(=78 tools)、`award_demo.py`(=PASS+CLEAN) 连成评审一键复现闭环，并标注实测期望 + "临时 .db 属正常、结尾自动 cleanup 仅剩交付库" 的说明；**所有期望均逐一实跑核对后写入**（不臆造）。
- **修正 README 一处超陈旧计数**：`## MCP 暴露面 → Tools（61 个）` → `Tools（78 个 · 精选概览；完整清单见 AGENTS.md 与 fist://map）`。
- 全仓README 工具计数 grep 复核：仅剩两处正确 78。
- 纯文档；`moon test --target js` **219/219**、`mcp_smoke` 78、`award_demo` PASS+CLEAN 不变。git：`b2aa561 → HEAD`。

## 获奖提升 · 第 26 轮：自驱取单 selfdrive_pick_next（按能力推荐自动认领）— 完成
- **把上轮"只读推荐"升级为"动作"**：新增 MCP 工具 **`selfdrive_pick_next`**（工具数 78→**79**）——对指定命名空间取 `task_triage` 顶部推荐并认领给 agent（**待领取→已领取**，能力匹配 `want` 优先），让无人值守可按能力推荐自推进。**不动看门狗主流程（零回归）**，只动一条推荐任务。
- **复用（避免造轮子）**：[engine_triage.mbt](../src/engine/engine_triage.mbt) 的 `pick_next` 直接复用 `triage`（排行+suggestion）与 `claim`（认领），无新表/依赖。
- **验证**：`moon test --target js` **220/220**（+1：能按推荐取走并认领 / 认领后无可领取不崩）；`award_demo.py` 加第 ⑩ 步 `selfdrive_pick_next`（认领 T0r106.1.2，remaining=29）**MCP-AWARD-DEMO PASS** + 结尾 CLEAN；`mcp_smoke` 79 工具 PASS。
- 文档即实现：工具 78→79、测试 219→220 全量同步（README/AGENTS/ARCHITECTURE/USAGE/deliverable/agent-map/scripts-README/mcp_smoke expected 79/申报书/server map 79）；agent-map 自驱(8)→(9) 补 `selfdrive_pick_next`。git：`0255e14 → HEAD`。

## 获奖提升 · 第 27 轮：award_demo 自推进闭环（推荐→取单→执行→验收→再推荐）— 完成
- **把"自驱取单"串成可看的一整圈**：`award_demo.py` 第 ⑪ 步对接续取中的任务**执行→提交→验收**，再 `task_triage` 重推荐——实证"推荐→取单→执行→验收→再推荐"闭环通（T0r106.2.1 完成，重推荐指向 T0r106.2.2），无人值守按推荐续取的具象演示。
- 纯脚本组合，无源码/工具/测试数变化（79 工具 / **220/220**）；结尾 cleanup **CLEAN**。
- 无新增工具，无需计数文档涟漪；仅 `scripts/award_demo.py` 变更。git：`a44a751 → HEAD`。

## 获奖提升 · 第 28 轮：脚本区整洁——清除临时残留 + rubric 正式化（项目整洁支柱）— 完成
- **审计发现**（`git ls-files` 比对 scripts/README 分类规则）：3 个 `_` 前缀临时文件被 `git` 跟踪残留——`_score_probe.py`（4-AI 门禁 dry-run 探测助手，plan 文档明确"跑完可删"）、`_probe_result.md`（探测输出，结论已固化进 score_gate.py 注释）、`_ai_prompt.md`。
- **整改（"临时脚本任务完清理 + 工具类辅助代码统一管理"）**：
  1. `git rm` 删除 `_score_probe.py`、`_probe_result.md` 两个真·临时产物；
  2. `git mv _ai_prompt.md → scoring_rubric.md`——它实为 `score_gate.py` 的 `DEFAULT_RUBRIC`（正式 4-AI 评分门禁的持久 rubric 资产），从临时代命名升级为「正式、可复用工具」（无前缀命名）；
  3. 同步 3 处引用：`score_gate.py` 两处（DEFAULT_RUBRIC、--rubric help）、源码注释，rubric 内"67 工具"→"79 工具 +3 resources +2 prompts / 220/220"（文档即实现）；
  4. scripts/README 分类表示例更新（删除已移除的两处 `_score_probe/_probe_result`）+ 新增 `score_gate+scoring_rubric` 正式工具说明 + 顺手修正陈旧工具计数 77/76/77→79。
- 历史 plan/spec/memory 文档保留原名的记录（不改历史）。`score_gate.py --help` 正常、`DEFAULT_RUBRIC` 解析到 `scripts/scoring_rubric.md`（存在）。
- 纯脚本/文档整洁，无 MoonBit 源码改动；`moon test --target js` **220/220** 不变、工具数不变（79）。git：`3e2255f → HEAD`。

## 获奖提升 · 第 29 轮：挑战题防漂移门禁（Challenger 变体过 Critic，SAGE/R-Few）— 完成
> 调研蒸馏 memory/research/ecosystem-borrow.md §五：SAGE 四专家环补最后一笔。R13 只让 Critic 审 principle/lesson、R14 的 Challenger 生成的挑战题自身未过差评门禁，故补"防课程漂移/同质坍缩"引入点。
- **实现（规则驱动、纯计算，与 evolve_critic 同源）**：`task_challenge` 新增可选 `critic`（DGM 档案库，缺省 None = 旧行为零回归）；开启时每个被生成挑战题先经 `critic_review`（同一单一真源）评审——与档案库既有资产重合 ≥ `critic_dup_sim()`(0.70) 判疑似课程漂移/同质坍缩 → 自动降档尝试其它策略（generalize→unhint→constrain→scale）；**全部策略皆漂移 → 拒发**（不出新根任务，返回 `Err` 带评审原因）。非漂移但稳健性偏低不阻断（挑战题本就偏新颖/高风险）。
- **实现点**：`engine_challenge.mbt` 重构 `challenge` 加 `critic? : @evolve.Archive?` 参数、新增 `pick_frame`（选帧+防漂移降档）与 `review_sim`/`ch_desc`；server `task_challenge` handler 加 `critic` bool 参数（开启时 `sync_db_lessons` 同步 DB lessons 再判全量）。工具数不变（79，仅加参数零回归）。
- **验证**：`moon test --target js` **223/223**（+3：空库放行 admitted / 当前策略漂移降档到 generalize / 全部漂移拒发且不出新任务）；E2E `task_challenge_verify.py` 追加 `critic=true`（MCP 层接受、返回 critic/critic_status=admitted、仍产出 [challenge]）**MCP-TASK-CHALLENGE-VERIFY PASS**；`tools/list=79`。
- **复用（避免造轮子）**：`critic_review`/`critic_dup_sim`/`Archive` 全复用 evolve 单真源，未再造词法/评分；`ch_desc` 提取为 pub 供测试与实现共用单一模板。
- 文档即实现：测试 220→223 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/项目申报书/scoring_rubric），README 徽章 220→223；AGENTS `task_challenge` 补 `critic=true` 说明；deliverable/申报书补 R29/§25 行。git：`01250c5 → HEAD`。

## 获奖提升 · 第 30 轮：执行者能力路由（Marketplace/Dynamic 范式雏形）— 完成
> 调研蒸馏 memory/research/ecosystem-borrow.md §一：Dynamic/Marketplace 范式（agent 注册能力 + router 按能力分配）已列为远期；`task_triage want` 只是"自选时偏好"，本轮补真正"按专长+负载分配"的路由原语。
- **实现（拿来主义：扩既有 executor 抽象，不造新表/依赖）**：
  - `src/executor/registry.mbt` 扩 `ExecutorRegistry`：加 `abilities: Map[String,Array[String]]` + `add_ability`/`abilities_of` + 纯函数 `split_need_tags`/`ability_coverage` + `route_pick(need, load)` 按 { 能力覆盖率 desc → 负载 asc → 名称 asc } 稳定排序；
  - server 加 2 工具（工具数 79→**81**）：`executor_register`（登记能力标签）、`executor_route`（按 need 能力 + 活跃负载路由，负载由 `executor_loads` 从 store 当前 assignee 活跃任务实时统计，进程内内存注册表）。
- **验证**：`moon test --target js` **227/227**（+4：need 切分 / 覆盖率 1.0·0.5·0.0·空=0 / 覆盖 desc→负载 asc 排序 / 同覆盖同名序）；E2E `scripts/executor_route_verify.py`（register 两执行者 → route(编排) 负载均衡到 exec-B / route(json) 专长路由到 exec-A + scratch 隔离）**MCP-EXECUTOR-ROUTE-VERIFY PASS**；`mcp_smoke` 81 工具。
- **复用（避免造轮子）**：直接扩既有 `executor` 包（原本只是未接线的 name→name map），把"注册表+纯计算路由"留在同包（单真源），server 只做编排（注入负载）；`executor_loads` 复用 `engine.list_all()`，无新表。
- 文档即实现：工具 79→81、测试 223→227 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/USAGE/scripts-README/mcp_smoke expected 81/项目申报书/scoring_rubric/server map 81）；AGENTS/agent-map 加「Marketplace·能力路由(2)」分组；mcp_smoke expected 79→81。git：`0c3291b → HEAD`。

## 获奖提升 · 第 31 轮：执行者能力注册持久化（跨进程可复现，Marketplace 补全）— 完成
> 闭环 R30 明确遗留："执行者能力注册为进程内内存、跨 server 重启不持久"→ 本轮让其与 `reserve_*`/`evolve` 一样落库（双后端 executors 表），补上"市场可真复现"短板。
- **实现（拿来主义：镜像 reservations 双后端模式，不造新范式）**：
  - store 层新增 `executors(name, abilities_json, created_at)` 表 + `executor_save`（ON CONFLICT DO UPDATE 幂等）/`executor_list`/`executor_clear`（SqliteStore + MemoryStore + StoreBackend 统一调度），`clear()` 一并清空（防测试/scratch 残留，对齐 R11）；
  - engine 加 `executor_save/list/clear` 透传；server 三工具（工具数 81→**82**）：`executor_register` 登记时持久化、`executor_route` 启动先 `sync_db_executors()` 回灌持久化注册（跨进程可复现）、新 `executor_clear`（重置/整洁）。
- **验证**：`moon test --target js` **230/230**（+3：executors 内存/双后端 save·list·覆盖幂等·clear + clear() 整库一并清空）；E2E `scripts/executor_route_verify.py` 重写为**跨进程三段**：进程A 注册 exec-RJX([json]) → 进程B（全新）`executor_route(json)` 读到并路由到 exec-RJX → `executor_clear` → 进程C 不再见 exec-RJX。**MCP-EXECUTOR-ROUTE-VERIFY PASS**（tools/list=82）。E2E 不创建任务，交付库零污染。
- **复用（避免造轮子）**：store 新增表完全复刻 reservations 的内外双后端 + clear 模式；路由纯计算仍在 executor 包单真源，engine 只做存/取/清透传。
- 文档即实现：工具 81→82、测试 227→230 全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/USAGE/scripts-README/mcp_smoke expected 82/项目申报书/scoring_rubric/server map 82）；AGENTS「Marketplace·能力路由(2)→(3)」补 `executor_clear`；deliverable/申报书补 R31/§27。git：`c98b291 → HEAD`。

## 获奖提升 · 第 32 轮：能力路由自动派单（selfdrive_dispatch，把"推荐"落成动作）— 完成
> 闭环 R30/R31：`executor_route` 只"推荐"、`selfdrive_pick_next` 按 want 自取——本轮补"把自己推荐真正分配出去"的动作，Marketplace 从"建议"到"动作"。
- **实现（复用 triage + executor_route + claim，无新表/依赖）**：新增 MCP 工具 **`selfdrive_dispatch`**（工具数 82→**83**）：先 `task_triage` 取顶部可领取任务 → 按 `want` 所需能力经 `executor_route` 找最佳执行者（覆盖率 desc→负载 asc）→ **直接 `claim` 给该执行者**（待领取→已领取）；无覆盖匹配回退 `agent`。返回 {dispatched, task_id, assignee, routed, route{best,coverage}}。
- **实现点**：纯 server 组合（`engine.triage` + `exec_reg.route_pick` + `engine.claim` + `executor_loads` + `sync_db_executors`），复用既有原语，单一真源在 executor 包。
- **验证**：`moon test --target js` **230/230** 不变（组合工具由 E2E 覆盖）；E2E `scripts/dispatch_verify.py`（登记 exec-DP([编排]) → publish 编排任务 → `selfdrive_dispatch(want=编排)` → routed=true 派给 exec-DP + 任务已领取 + executor_clear 复位）**MCP-DISPATCH-VERIFY PASS**（tools/list=83）。E2E 会向交付库写一条演示任务，运行完以 `git checkout -- fist-mbt.db` 恢复整洁 + cleanup --check CLEAN。
- 文档即实现：工具 82→83（测试数不变 230）全量同步（README/AGENTS/ARCHITECTURE/deliverable/agent-map/USAGE/scripts-README/mcp_smoke expected 83/项目申报书/scoring_rubric/server map 83）；AGENTS/agent-map「Marketplace(3)→(4)」补 `selfdrive_dispatch`；deliverable/申报书补 R32/§28。git：`bbd130f → HEAD`。

## 获奖提升 · 第 33 轮：派单免手传（selfdrive_dispatch 自动抽取所需能力）— 完成
> 闭环 R32：`selfdrive_dispatch` 需调用方手传 `want` 才做能力路由——本轮让它真正"hands-free"：不传 want 时从任务描述自动抽取已注册执行者的能力标签即为 need。
- **实现（纯 server 助手 + 复用）**：新增私有 `auto_need(desc)`——扫任务描述里出现的各已注册执行者能力标签，取最长的那个作 need（最具体）；`selfdrive_dispatch` 在 `want` 空时用它，返回 `need_auto=true` 标记；显式 `want` 仍优先（零回归）。
- **验证**：无工具/测试数变化（83/230，同工具增强，由 E2E 覆盖）；`dispatch_verify.py` 追加免手传用例：另发一条「编排调度器」任务 → `selfdrive_dispatch`(不传 want) → `need_auto=true`、自动抽取 `want=编排`、仍派给 exec-DP、任务已领取。**MCP-DISPATCH-VERIFY PASS**。运行完 `git checkout -- fist-mbt.db` 恢复 + cleanup --check CLEAN。
- 复用/整洁：`auto_need` 复用 `exec_reg.list`/`abilities_of`（单真源），`String::contains` 最简抽取（与覆盖语义一致）；AGENTS `selfdrive_dispatch` 行补 R33 免手传说明。工具/测试计数不变，仅行为增强页。git：`6b810cd → HEAD`。

## 获奖提升 · 第 34 轮：README 测试徽章一致性守卫（CI 徽章"实时化/不同步门禁"，项目整洁/工具类管理）— 完成
> 审计缺口：README 徽章 `tests-N%2FN` 是手写静态数字，此前每轮都需手工同步（且易漏）；把"计数一致"变成 CI 硬门禁，杜绝"徽章过时/文档即实现倒退"。
- **新增 `scripts/check_badge.py`**：读 `moon test --target js` 日志解析实际 Total/Passed/Failed，解析 README 徽章 `tests-N%2FN`，不一致（或 failed>0）退出码 1。
- **接入 `.github/workflows/ci.yml`** 的 JS 轨道：`Test (js)` 改为 `set -o pipefail; ... | tee /tmp/moon_test.log`，其后加 `Badge consistency guard` 步骤 `python3 scripts/check_badge.py /tmp/moon_test.log README.md`——用 pipefail 保住测试失败的 CI 信号，同时把徽章同步交给 CI 校验。
- **验证**：本地实跑 `moon test --target js -j 1` + `check_badge.py` PASS（230/230==230/230）；负路径用伪造 `tests-231%2F231` README 复现 **BADGE-STALE 退出码 1**（能抓出过期徽章）。
- 无工具/测试数变化（83/230，CI 门禁 + 脚本，无文档计数涟漪）；scripts/README 补 `check_badge.py`；cleanup --check CLEAN。git：`9ac2d68 → HEAD`。

## 获奖提升 · 第 35 轮：LADDER 递归拆解深化（gradient 提示补"先易后逆推"自举闭环）— 完成
> 审计 R12/R17：`gradient=true` 已给子任务贴难度梯度 + "更简单变体"提示，但只是"标签/单句"，未真正引导执行者"先生成更简单变体→完成→逆推原题"的 LADDER 闭环。贴合用户第一支柱"递归拆解复杂任务"。
- **深化（单点、纯文本、零回归）**：`src/decompose/decompose.mbt` 的 `simpler_variant_hint` 扩为"由易到难——先完成该更简单变体，再推广/逆推到本任务目标（LADDER 先易后逆推）"，把"贴标签"升级为"把更简单变体当作第一步、再逆推原题"的具体执行指引。
- **验证**：`moon test --target js` **230/230**（在既有 gradient 测试内补断言：子任务描述含"先易后逆推"与"推广/逆推"，证明深化提示真实传播；测试计数不变）。默认关闭零回归。
- 复用/整洁：单点改纯函数 `simpler_variant_hint`，engine 透传不变；AGENTS `task_plan_deep` 行补"更简单变体 → 先易后逆推 LADDER 自举"；工具/测试计数不变（83/230）。git：`a86c37d → HEAD`。

## 获奖提升 · 第 36 轮：项目地图 fist://map 全量同步（第一支柱"agent 首读即有地图一目了然"扫尾）— 完成
> 审计第一支柱缺口：`fist://map` 的 `tool_groups` 仍停留在老分组——缺 看板/脉冲/预订/推荐+DAG、自驱缺 selfdrive_pick_next、记忆缺 evolve_critic/lesson/task_challenge、attro/**Marketplace·能力路由(executor_*/selfdrive_dispatch)整组缺失**。agent 首读地图会找不到新工具，正撞"让 agent 有地图、别全项目找"。
- **同步（纯 JSON 地图、零代码/计数变化）**：`src/server/server.mbt` 的 `fist://map` `tool_groups` 补全到当前 83 工具的 10 大分组（生命周期/查询/运维/看板·脉冲·预订·推荐+DAG/自驱/记忆与自进化/Marketplace·能力路由/审计权限/多租户/强验证），与 AGENTS/agent-map 分组对齐。
- **验证**：`map_verify.py` 追加断言（地图须含 Marketplace·能力路由 与 看板/脉冲分组、含 executor_register），**MCP-MAP-VERIFY PASS**（tool_groups=[…10 组]）；`moon test --target js` **230/230** 不变。
- 复用/整洁：地图仍是单处 JSON 单一真源；`executor` 包名已出现在 packages。工具/测试计数不变（83/230）。git：`c041808 → HEAD`。

## 获奖提升 · 第 37 轮：评审证据表全量对账（deliverable/申报书轮表补 R33-R36，文档即实现）— 完成
> 审计：`docs/deliverable.md` §四 停留在 R32（缺 R33-R36）、`项目申报书.md` 轮表停在 R28（缺 R29-R32）——评审对照的"每轮增强+验证脚本"证据链断开。
- **对账（纯文档、零代码/计数/风险）**：deliverable §四 header「git 4994aae → HEAD，32 轮→36 轮」+ 追补 R33(派单免手传)/R34(徽章守卫)/R35(LADDER深化)/R36(地图同步) 四行；申报书追补 R29-R32 四行。
- **验证**：无源码/工具/测试变化（83/230）；仅文档核对。git：`8ecef4f → HEAD`。

## 获奖提升 · 第 38 轮：CI 仓库整洁守卫（cleanup_artifacts --check 接入 JS 轨，项目整洁硬门禁）— 完成
> 补 R34 之后第二道 CI 门禁：R34 管"徽章≠实测"，本轮管"仓库是否只留交付库"—每 push 都强制"除 fist-mbt.db 外无 *.db / temp/ 无残留"，把项目整洁（用户第三支柱）变成 CI 硬约束。
- **接入（纯 CI、零代码/计数变化 83/230）**：`.github/workflows/ci.yml` JS 轨在 badge guard 后加 `Cleanliness guard` 步骤：`cleanup_artifacts.py && cleanup_artifacts.py --check`（前者删测试/演示残留，后者断言仅 fist-mbt.db，非 0 退出即 FAIL）。
- **验证**：本地 `cleanup_artifacts.py --check` 退出码 0（CLEAN）；无源码改动。git：`ac538ad → HEAD`。
> 注：本轮为功能轮，字母轮号接 R38 续为 **R39**；deliverable 功能轮表计为第 37 行。

## 获奖提升 · 第 39 轮：LADDER 真实 DAG 前驱链（gradient_dag）— 完成
> 延续 R35「先易后逆推」提示，本轮把它从"描述里的提示文本"升级为**真实可领取门禁**：	ask_plan_deep 新增可选参数 gradient_dag（须 gradient=true 生效，默认关闭零回归）；开启后每层拆解把兄弟切片按"由易到难"连成 depends_on 前驱链——后续更难切片必须等它更简单的前驱切片完成后才可认领，经 dag_ready/dag_ascii/topo_sort 全部可见。这是"更简单变体"真正落在 DAG 上，而非仅提示文案。
- **实现**：[engine.mbt](../src/engine/engine.mbt) plan_deep + decompose_rec 透传 gradient_dag，子任务创建即带 depends_on（非首切片依赖前一兄弟），返回树节点附 depends_on 便于断言；[server.mbt](../src/server/server.mbt) 	ask_plan_deep 加 gradient_dag schema 与解析透传。未新增工具（83 不变）。
- **验证**：moon test --target js **231/231**（+1 新测试，control 组断言仅 gradient 时兄弟无依赖、实验组断言前驱链与落库一致）；check_badge.py PASS（README 231 == 实测）；mcp_smoke.py PASS 83 工具；moon check 0 错误。
- **联动**：README/AGENTS/deliverable/scoring_rubric 230→231 全量同步；deliverable §四 补 37 行。
> 注：本轮为功能轮，字母轮号接 R39 续为 **R40**；deliverable 功能轮表计为第 38 行。

## 获奖提升 · 第 40 轮：「执行计划视图」（task_plan_deep 新增 exec_order）— 完成
> 接 R39 gradient_dag（真实 DAG 前驱链），本轮把它真正"用起来"：拆解后直接吐一张计划。
- **实现（engine 层纯读新函数，不加工具/计数不变）**：[engine.mbt](../src/engine/engine.mbt) 新增 plan_exec_order(root_id)——BFS 收集整棵子树全部后代 → 按 depends_on 拓扑排序 → 输出扁平执行清单（每条 id/depth/难度档/依赖/状态），难度档从子任务描述 [难度梯度 idx/n:档] 抽取（ind_sub_chars+xtract_difficulty 纯 Char 扫描，避开 index_of/substring 签名）；纯读无副作用。
- **server 端**：[server.mbt](../src/server/server.mbt) 	ask_plan_deep 返回对象新增顶层 xec_order 字段，agent 拆完即可照单执行。
- **验证**：moon test --target js **232/232**（+1，DAG 序不变量：任何任务其 depends_on 前驱在序中下标必更小，且每后代带难度档）；check_badge.py PASS（232）；mcp_smoke.py PASS 83 工具；moon check 0 错误。
- **联动**：README/AGENTS/deliverable/scoring_rubric 231→232 全量同步；deliverable §四 补 38 行。
- **自演进**：与 gradient_dag 协同——先完成依赖前驱、由易到难，体现在一张可执行计划里，契合"fist-mbt 是更好的 AI 项目管理工具"核心定位与用户支柱①"计划一目了然"。
> 注：本轮为功能轮续号 **R41**；纯演示串演，测试/工具计数不变（232/83）。

## 获奖提升 · 第 41 轮：award_demo 串演「真实 DAG + 执行计划」（R39/R40 纵向可见）— 完成
> 把最新两块能力接进评审一条命令的 DEMO，让"更简单变体→真实 DAG→执行计划照单执行"成为可见可复现的链，而非只可调用。
- **实现（仅改 scripts/award_demo.py，加 ③b 演示段，不动核心/不计数）**：在既有 gradient 拆解后，另开 scratch 根任务用 gradient=true, gradient_dag=true 拆解，读取返回的 xec_order 打印执行计划前 4 步与步数，并断言非空——把 R35(gradient 提示) / R39(gradient_dag 真实 DAG) / R40(exec_order 执行计划) 串成一条可见链。
- **验证**：python scripts/award_demo.py **MCP-AWARD-DEMO PASS**——新增 ③b 段输出 "exec_order 9 步执行计划：T0r145.1 → T0r145.1.1 → T0r145.2.1 → T0r145.3.1…（附难度档/依赖/深度，照单执行）"，后 ⑨⑩⑪ 推荐→取单→执行→验收→再推荐整圈仍通；结尾 cleanup_artifacts 移除 66 生成物并对仓库根 --check 判定 CLEAN。
- **自演进**：	ools=83 一致；测试仍 232/232（无代码语义变化）。
> 注：本轮为功能轮续号 **R42**；deliverable 功能轮表计第 39 行。

## 获奖提升 · 第 42 轮：task_triage 携带真实 DAG 依赖 depends_on（复用/拿来主义支柱）— 完成
> 把 R39 建的 DAG 依赖边复用到"下一单推荐"：	ask_triage 每条推荐任务新增 depends_on 输出，"下一单"不止看难度/优先级，还能看到它就绪的前驱是谁——DAG→推荐纵向闭合，落实用户支柱②"能复用则复用"。
- **实现**：[engine_triage.mbt](../src/engine/engine_triage.mbt) TriageRow 加 depends_on 字段，循环里取 	.depends_on 并在 JSON 输出；未新增工具（83 不变）。
- **验证**：moon test --target js **233/233**（+1，双根 + dag_depend：依赖未满足时不在推荐、依赖前驱完成后入推荐且暴露 depends_on）；check_badge.py PASS（233）；mcp_smoke.py PASS 83 工具；moon check 0 错误。
- **联动**：README/AGENTS/deliverable/scoring_rubric 232→233 全量同步；deliverable §四 补 39 行；AGENTS 	ask_triage 行补"每条含 depends_on"。
> 注：本轮为功能轮续号 **R43**；纯去重重构，测试/工具计数不变（233/83）。

## 获奖提升 · 第 43 轮：triage 难度抽取单一化（复用 R40 extract_difficulty，去重）— 完成
> 落实支柱②"复用/避免重复造轮子"：triage 自己的 :易] 后缀粗匹配与 R40 的 xtract_difficulty 是两套难度解析，本轮统一复用后者。
- **实现（仅 engine_triage.mbt）**：	riage_label_of 改为调用 xtract_difficulty（同包模块可见）再归一化易/中/难；顺带修正 calibrate 真实难度标签（难 d=5）无法被旧 :易] 后缀匹配的问题。空/无标签仍回退"叶/分支"兜底，行为对既有用户零回归。
- **验证**：moon test --target js **233/233**（无计数变化；既有 	ask_triage 返回可领取排行并带难度标签 用例回归通过）；moon check 0 错误。
- **调试教训**：triae 只列"待领取"任务，顶层非叶孩子在递归时已置为'拆分中'不入 triage，故"calibrate 难度在 triage 显示"的端到端断言前提不成立——改为依赖既有用例回归 + 纯函数语义收敛，避免误造测试。
- **联动**：deliverable §四 补 40 行。
> 注：本轮为功能轮续号 **R44**；deliverable 功能轮表计第 41 行。

## 获奖提升 · 第 44 轮：status_summary 难度分布 by_difficulty（复用/一目了然）— 完成
> 结合支柱①"一目了然"与②"复用"：项目脉冲新增待领取任务难度结构。
- **实现**：[engine.mbt](../src/engine/engine.mbt) xtract_difficulty 升 pub（跨包可复用）；[board_ascii.mbt](../src/server/board_ascii.mbt) ender_status_summary 增 y_difficulty:{易/中/难/无:数量}（仅统计"待领取"任务，复用 @engine.extract_difficulty）；[server.mbt](../src/server/server.mbt) status_summary 工具描述同步。
- **验证**：moon test --target js **233/233**（延展既有用例加 by_difficulty 不变量：各难度档之和==待领取数）；mcp_smoke.py PASS 83 工具；check_badge.py PASS；moon check 0 错误。
- **联动**：AGENTS status_summary 行、deliverable §四 补 41 行。
> 注：本轮为演示增强续号 **R45**；纯脚本，计数不变（83/233）。

## 获奖提升 · 第 45 轮：award_demo 难度一路可见（exec_order 每步难度 + by_difficulty 分布）— 完成
> 把 R39/R40/R44 的难度链在评审一条命令里端到端可见：拆解计划每步带难度档，脉冲展示待办难度分布。
- **实现（仅 scripts/award_demo.py，改 ③b）**：执行计划步串改为 id[难度]（T0r151.1[易]→…1.1[易]→…2.1[中]→…3.1[难]），新增 ③c 调 status_summary 打印 by_difficulty（易/中/难/无分布）。纯演示层，零风险、无计数变化。
- **验证**：python scripts/award_demo.py **MCP-AWARD-DEMO PASS**——③b 每步难度可见、③c 显示"易=19/中=18/难=20/无=0"，后主流程整圈仍通；结尾 cleanup --check CLEAN。
> 注：本轮为功能轮续号 **R46**；deliverable 功能轮表计第 42 行。测试/工具计数不变（83/233）。

## 获奖提升 · 第 46 轮：工具清单单一真源守卫 + AGENTS 补齐 29 工具（项目整洁支柱③ 架构性）— 完成
> 把"文档即实现"对工具清单做成立即可验的 CI 硬约束：唯一真源 = server.mbt 实际注册。
- **新守卫**：新增 [scripts/check_tools_sync.py](../scripts/check_tools_sync.py)——从 server.mbt 用正则 instrumented_tool(\n s1,\n "<name>",) 精确提取 83 个工具名（实测无重复），校验 ①AGENTS 表格行首反引号名 ⊆ 真源（防文档出现不存在的工具）②真源全部工具必须出现在 AGENTS（防新增工具忘写文档）③README/AGENTS/deliverable/scoring_rubric 工具总数表述 == 83。接入 ci.yml JS 两轨（ubuntu + windows）。
- **脚手架即发现缺口**：守卫实测发现 AGENTS.md 只列了 54/83 个（漏 29 个真源工具）——真实"文档即实现/项目整洁"漂移。逐组补齐：生命周期 12→14（publish_parallel/reopen_task）、新增「自驱闭环(9)」「运维·日志/缺陷/成本/调度(9)」「衍生子项目·ATGC-old(3)」三组、自进化 7→11（evolve_sample/snapshot/submit/asset_register）、Omega 补 omega_verify/verify_fix。补齐后守卫 PASS。
- **验证**：python scripts/check_tools_sync.py **PASS**（83 对齐）；moon test --target js **233/233**；moon check 0 错误；工具计数仍 83。
- **联动**：AGENTS.md、ci.yml、deliverable §四 补 42 行；README "83 个 MCP 工具" 与守卫核一致。
> 注：本轮为功能轮续号 **R47**；deliverable 功能轮表计第 43 行。测试/工具计数不变（83/233）。

## 获奖提升 · 第 47 轮：测试总数单一真源守卫（补 check_badge 盲区）— 完成
> 收紧"文档即实现"最后一块：check_badge 只核 README 徽章 vs 日志，但 AGENTS/deliverable/scoring_rubric 的测试总数漂移无人管。新增 [check_test_sync.py](../scripts/check_test_sync.py) 跨四文档核对实测测试总数，接入 ci.yml JS 轨。
- **实现**：[check_test_sync.py](../scripts/check_test_sync.py)——从月亮日志 Total tests: N 提取实测总数，校验 README/AGENTS/deliverable/scoring_rubric 四处的 N/N、全绿、独立 N 任一表述与实测一致；--total 直传亦可。
- **验证**：正路径 --total 233 **PASS**；负路径 --total 999 **FAIL 退出 1**（四文档全缺），守卫有效。
- **联动**：ci.yml JS 轨加 Test-count guard（紧跟 Tool-list guard）；deliverable §四 补 43 行。
> 注：本轮为功能轮续号 **R48**；deliverable 功能轮表计第 44 行。测试/工具计数不变（83/233）。

## 获奖提升 · 第 48 轮：fist://map 补全工具家族 + map_verify 锁定（支柱① 地图对 agent 完整）— 完成
> 环视发现：fist://map 的 tool_groups 此前把 R46 已列的新增工具（publish_parallel/reopen_task/omega_verify/verify_fix/运维杂项9/atgc_old3 等）漏掉了——agent 首读地图却不完整，违反支柱①。
- **实现（仅 server.mbt map_json + map_verify.py，不动工具/计数）**：[server.mbt](../src/server/server.mbt) map_json tool_groups 补：生命周期加 publish_parallel/reopen_task、强验证加 verify/verify_fix、新增「运维·日志/缺陷/成本/调度(9)」「衍生·ATGC-old(3)」两组、自驱组补 selfdrive_dispatch；[map_verify.py](../scripts/map_verify.py) 加 R48 断言锁住这些关键补漏（正/误判路径）。
- **验证**：moon test --target js **233/233**（server 改动无回归）；python scripts/map_verify.py **MCP-MAP-VERIFY PASS**（12 分组 + 关键补漏均命中）；check_tools_sync.py PASS（83 对齐）。
- **注**：地图既有的"人类友好短名"分组（如 audit_permission/log）不代表工具 id 全覆盖——精确全覆盖是 check_tools_sync(AGENTS vs server) 的职责，故 map_verify 用"关键补漏命中"而非"精确 id 全覆盖"断言。
- **联动**：deliverable §四 补 44 行。

## 获奖提升 · R49：文档收尾快照（申报书/scripts-README 对齐 HEAD，文档即实现）— 完成
> 支柱③"文档即实现"：把历史遗留的陈旧计数/分组从非历史文档里清掉，兑现申报书"按 HEAD 实时校准"声明。
- **修正 项目申报书.md**：230→233、220 项→233 项、10 分组→12 分组、6 轮→多轮；Self-check 复核陈旧数 0 处；轮表 32 后补一行指向 deliverable §四（至第 44 行，233/233 基线）。
- **补 scripts/README.md**：登记 R46 check_tools_sync.py、R47 check_test_sync.py 两条守卫说明（工具类辅助文档统一管理）。
- **验证**：check_tools_sync.py PASS、check_test_sync.py --total 233 PASS；无代码/测试/工具计数变化（83/233）。
- **联动**：非功能轮 → deliverable 功能轮表不增行（保持 44），并入 §五文档即实现说明。

## 获奖提升 · R50：README 评审自检注释校准（230→233，评审可复现）— 完成
> 支柱③"文档即实现"的细颗粒修正：README「一键完整自检（评审用）」注释里的 Total tests: 230 已陈旧（现 233），评审照抄注释会看到矛盾。
- **修正 README.md**：自检块 Total tests: 230, passed: 230 → 233, passed: 233；其余 mcp_smoke → 83 工具、ward_demo → PASS 两条已实测一致。
- **实测复核**：本会话 moon test 233/233、mcp_smoke PASS、ward_demo PASS 均已跑过，三条命令确实全绿，评审照命令可复现。
- **验证**：check_test_sync --total 233 PASS、check_tools_sync PASS、README 陈旧 230 出现次数 0。
- **联动**：非功能轮 → deliverable 功能轮表不增行。
> 注：本轮为功能轮续号 **R51**；deliverable 功能轮表计第 45 行。工具计数不变（83），测试 233→234（+1）。

## 获奖提升 · 第 51 轮：看门狗无人值守派发预览（R51，自治闭环前置信号）— 完成
> 贴核心叙事"fist-mbt 更好 AI 项目管理"的自治推进：watchdog_tick 在显式 ns 无人值守场景新增 detail.ready_dispatch_preview——复用 engine.triage 给出一台"下一单可自动派发"的候选（纯读、不自动认领）。
- **实现（仅 ops_watchdog.mbt + 测试）**：watchdog_tick 构建 detail 时，ns 非空则调用 ngine.triage(heal_ns,"watchdog","") 取其 suggestion 压缩为 {task_id,note,hint}（hint 指明可对这条调 selfdrive_dispatch/schedule 做能力路由自治派单）。复用既有 engine.triage，零 server 耦合。
- **验证**：moon test --target js **234/234**（+1 新用例，ns 下预览指向可派发的 T0）；moon check 0 错误；mcp_smoke PASS 83 工具；check_test_sync --total 234 PASS；check_tools_sync PASS。
- **设计取舍**：完整"看门狗自动 selfdrive_dispatch"需把 server 进程内 xec_reg 派单下沉到 engine 层（跨包注入），中改回归风险高——本轮以只读预览落"自治预备信号"，避免盲目激进集成，派单下沉留作被记录的后置项。
- **联动**：README/AGENTS/deliverable/scoring_rubric 233→234 全量同步；deliverable §四 补 45 行；AGENTS watchdog_tick 行补预览说明。

## 获奖提升 · R52：check_badge 防自检注释陈旧（R50 手动修固化）— 完成
> R50 手动修了 README 自检注释 230→233；本轮把它固化成自动门禁，杜绝该类漂移复发。
- **实现（仅 scripts/check_badge.py）**：新增 parse_readme_selfcheck_count，解析 README「一键完整自检」块注释 # → Total tests: N, passed: N；在既有徽章对比后，若 N≠实测则 SELFCHECK-STALE 退出 1（徽章为权威，自检注释次之；徽章过、注释旧也会被抓）。
- **验证**：正路径 check_badge.py <log> README.md **PASS 徽章 234==实测 + 自检注释 234==实测**；负路径（临时 README 自检注释写 233）**SELFCHECK-STALE 退出 1**——守卫对 R50 类漂移敏感。
- **联动**：非功能轮（无代码/测试/工具计数变化，83/234）；scripts/README 的 check_badge 行可在下次同步时补"含自检注释核对"。

## 获奖提升 · R53：评审一链自检复现审计（cmd/cli + servers + 守卫全绿且隔离）— 完成
> 支柱④"他人可直接使用/结果可复现"的一次实证审计：把 README「一键自检」整条链逐命令真跑一遍，确认无漂移、无污染。
- **审计项**：① moon run cmd/cli → 实测打印"发布成功/认领成功/拆分成功 3 个子任务"+ 共 4 任务；确认 @engine.FistEngine::new() 为内存引擎 → 一键自检**不写交付库 fist-mbt.db**（只打印内存中的 4 个任务），隔离干净。② mcp_smoke PASS 83 工具。③ ward_demo PASS。④ check_badge（含自检注释）/check_test_sync/check_tools_sync 全 PASS。
- **验证**：cleanup_artifacts.py 移除 66 生成物、保留交付库；git status 干净；moon test 234/234。
- **结论**：README 自检链（cmd/cli 隔离 + mcp_smoke/award_demo/守卫全绿）真实可复现，评审照命令即可；无需要修的薄弱点。

## 获奖提升 · R54：清理 cmd/cli 未使用 store 依赖（消 unused warning，整洁收口）— 完成
> R53 审计发现并随手消掉一处 compile warning：cmd/cli/moon.pkg 声明了未使用的 store 依赖（unused_package warning）。
- **实现（仅 cmd/cli/moon.pkg）**：从 import 移除 icTop-cw/fist-mbt/src/store（CLI 走 @engine.FistEngine::new() 内存引擎，不直接依赖 store）。
- **验证**：moon check 0 错误且该 unused_package 警告消失；moon run cmd/cli 仍打印 4 任务正常；moon test --target js **234/234** 无回归。
- **联动**：非功能轮（零工具/测试/文档计数变化，83/234）。
> 注：本轮为功能轮续号 **R55**；deliverable 功能轮表计第 46 行。工具计数不变（83），测试 234→235（+1）。

## 获奖提升 · 第 55 轮：看板每行标注难度档（board_ascii 复用难度单一来源）— 完成
> 支柱①"一目了然"＋②"复用"合流：实时任务看板不再是纯状态+缩进，而是每行附自身难度档。
- **实现（仅 board_ascii.mbt + 测试）**：ender_board_ascii.append_group 每行任务经 @engine.extract_difficulty（R44 已 pub 的单一来源）归为 易/中/难 并追加  难度[X]；无难度（非 gradient/legacy）不标注、不增噪。
- **验证**：moon test --target js **235/235**（+1 新用例，gradient 拆解后 ascii 含 "难度["）；moon check 0 错误；三守卫 PASS。
- **联动**：README/AGENTS/deliverable/scoring_rubric 234→235 全量同步；deliverable §四 补 46 行；AGENTS board_ascii 行补"每行标注难度档"。
- **自演进**：与 R44 status_summary by_difficulty 形成"脉冲看分布 + 看板看逐条"的难度全貌闭环。
> 注：本轮为功能轮续号 **R56**；deliverable 功能轮表计第 47 行。工具计数不变（83），测试 235→237（+2）。

## 获奖提升 · 第 56 轮：引擎层自治派单 primitive（dispatch_next，store-backed，自治闭环地基）— 完成
> 终于没有回避"自治叙事"的实质工程：把派单下沉到 engine 层，做成**纯增量、零回归**的 primitive，为看门狗自动派单铺路。
- **实现（新增 engine_dispatch.mbt + engine→executor 依赖 + 2 用例）**：FistEngine::dispatch_next(ns, agent, want, now)——取 	riage 顶部可领取任务 → 读 store 持久化执行者能力注册（executor_list）→ executor 包 oute_pick 按「能力覆盖→负载」路由最佳执行者（复用单真源路由，不重复造）→ 直接认领给最佳执行者（无注册回退 agent）。未改任何既有工具/看门狗行为（零回归），未起 watchdog（只落 engine primitive）。
- **验证**：moon test --target js **237/237**（+2：注册 worker-A 能力→派发并认领给 worker-A；空仓→不派发不改动）；moon check 0 错误；三守卫 PASS。
- **耦合结论**：executor 包仅依赖 core+json（叶包），engine→executor 无循环；engine 本可读 store 执行者，故"派单下沉"其实不依赖 server 进程内 exec_reg——之前担心的耦合是虚的。
- **联动**：README/AGENTS/deliverable/scoring_rubric 235→237 全量同步；deliverable §四 补 47 行。

## 获奖提升 · 第 57 轮：看门狗自治派单接入（watchdog_tick + autodispatch → dispatch_next）— 完成
> 自治闭环闭环：把 R56 的 engine 层派单 primitive 接进 watchdog_tick，无人值守下"该派哪单即可自动派给谁"全打通。
- **实现（仅 ops_watchdog.mbt + 测试）**：watchdog_tick 新增 `autodispatch=false`（默认关闭，零回归）与 `autodispatch_want`：显式 ns 无人值守且无活跃任务（idle）时调 engine `dispatch_next(heal_ns, next_created_by, autodispatch_want, now)`，把顶部待领取任务按能力/负载认领给最佳执行者，结果并入 `detail.autodispatch`。不改既有 waiting/idle/advanced/restarted 语义。detail 由 `let` 改 `var`，仅 idle 分支覆盖 autodispatch 字段。
- **踩坑**：`route_pick` 对空 want 走 cover=0 的名称/负载兜底，且注册表来自 Map 迭代顺序不确定，会把 single store 执行者也混入候选——所以加 `autodispatch_want` 显式传能力标签，正例路由才能稳定命中 worker。
- **验证**：moon test --target js **239/239**（+2：autodispatch=true+ns 待领取+store 执行者→认领给 worker-57；autodispatch=false→不认领不改动）；moon check 0 错误；三守卫 PASS；mcp_smoke 83 工具；cleanup --check 清理后洁净。
- **联动**：README/AGENTS/deliverable/scoring_rubric 237→239 全量同步；deliverable §四 补 48 行；AGENTS watchdog_tick 行补 autodispatch/autodispatch_want 参数。

## 获奖提升 · 第 58 轮：派单能力自动抽取（dispatch_next want 免手传）— 完成
> 把 R57 看门狗自动派单的"最后一处手动"去掉：want 不用显式传，从任务描述自动抽。同时补 R57 漏掉的 AGENTS watchdog_tick 文档（自治派单写实）。
- **实现（仅 engine_dispatch.mbt + 测试）**：新增 `FistEngine::auto_want(desc)`（store-backed，复用 R33 server 层 `auto_need` 思路但读 store 执行者注册、不依赖进程内 exec_reg）：扫描述里出现的 store 持久化能力标签、取最长命中。`dispatch_next` 在 want=="" 时对顶部任务描述 auto_want，route_pick 用抽取出的 want_eff；返回 JSON 补 `want` 字段。显式 want 仍优先（既有用例零回归）。
- **验证**：moon test --target js **240/240**（+1：want 空→描述含"仪表"→自动抽取并路由命中 nete）；moon check 0 错误；三守卫 PASS；mcp_smoke 83 工具；cleanup 洁净。
- **文档漂移修复**：R57 只改 AGENTS 测试计数行，watchdog_tick 行仍写"纯读不认领"——本轮补 autodispatch=true 自治派单 + autodispatch_want 缺省自动抽取。
- **联动**：README/AGENTS/deliverable/scoring_rubric 239→240 全量同步；deliverable §四 补 49 行。
- **意义**：自治闭环真正"零参数"——无人值守下只要你给任务描述与执行者能力注册，看门狗就能自动决定"派给谁"。

## 获奖提升 · 第 59 轮：4-AI 概率门禁复跑（新档位 70/85/97）——首跑 FAIL→触发自我迭代
> 用户更新 scoring_rubric 强化「4-AI 概率自评分门禁」，本轮据此在当前快照复跑门禁，暴露真实短板。
- **首跑 FAIL**：AI1 0.72/0.87/0.97 pass；**AI2(LongCat) fail 0.62/0.82/0.95**（全档差于阈值，创意生态 p1 与三等奖 p3 最低）；AI3 0.75/0.90/0.98 pass；AI4 0.72/0.87/0.97 pass。AND 聚合=FAIL。
- **诊断**：非项目回归（其余 3 AI 过线）；根因 = 最严苛评分者对「创意生态/美观演示/三等奖确定性」证据不足——与历史 AI2 短板一致（「创意缺采用证据 + 美观缺视觉层」）。
- **atomcode 命令修正**：首用 `--model "longcat/LongCat-2.0"` 报 HTTP400 Unsupported；正确组合 `--model "LongCat-2.0" --provider longcat`（AI2）、`--model "kimi-k3" --provider Kimi`（AI4）、默认（AI3），`--no-tools --ephemeral --prompt-file`。
- **留痕**：memory/research/score-20260924.md 记第 8 次评估（FAIL）。

## 获奖提升 · 第 60 轮：视觉演示 + 自举采用证据增强 → 门禁复评 PASS（首过）
> 对症补 AI2 短板维度（美观/演示/采用证据），历史杠杆：当年同样把最严苛者 p1 抬过线。
- **实现**：`showcase.ps1` 新增「自治派送 AUTONOMOUS DISPATCH」视觉段（triage→dispatch_next→executor_route→watchdog autodispatch 零参数链），修正陈旧计数 67/184→**83/240**；实测 exit 0 / 1.7s 渲染，读盘真实自举证据（**548 tasks / 196 executions / 6 review 存档**）。`_snapshot.md` 补「自举采用证据」与视觉/生态强化（快照是门禁喂的所有 AI 的唯一事实依据）。
- **复评 = PASS=是**：AI1 0.72/0.87/0.97；**AI2 → pass 0.72/0.87/0.98**（fail 0.62→0.72）；AI3 0.73/0.88/0.98；AI4 0.71/0.86/0.97。全部过 70/85/97。
- **状态**：新档位**首过**，按既定口径需**复评一次确认稳健**（历史惯例连续两次 PASS 才判达标）；复评留待下一步。临时快照 `_snapshot.md` 保留待复评，完成后清理。
- **留痕**：score-20260924.md 记第 9 次评估（PASS 首过）。

## 获奖提升 · 第 61 轮：门禁复评确认——新档位连续两次 PASS，概率门禁兜底成立
> 上轮 PASS 属"首过"，按惯例需复评确认稳健；本轮完成复评。
- **复评 = PASS=是（同快照）**：AI1 0.72/0.87/0.97；AI2 0.72/0.86/0.97；AI3 0.72/0.87/0.97；AI4 0.72/0.88/0.98。4 AI 全部三档均衡接近（p1 全 0.72、p2 0.86-0.88、p3 0.97-0.98），**无临界随机性**。
- **结论**：新档位（一等70/二等85/三等97）连续两次实跑 PASS=是，概率门禁兜底成立——R60 的视觉演示+自举采用证据把最严苛者 AI2 从 fail(0.62/0.82/0.95) 抬到稳定 pass。
- **清理**：临时快照 `memory/research/_snapshot.md` 删除（留痕已含数据，遵循临时文件即用即清，支柱③）。
- **留痕**：score-20260924.md 记第 10 次评估（PASS 确认）。
- **后续候选**：概率目标达成，可转向申报书完善 / 评审会 demo 打磨 / 三支柱其余薄弱点（复用增强、项目整洁纵深），或进一步把门禁阈值/证据做成 CI 或 cron 持续门禁。

## 获奖提升 · 第 62 轮：工具类辅助代码统一管理——scripts/README 单一索引守卫
> 转向三支柱③「项目整洁」纵深：把 scripts/ 34 个辅助脚本视为"工具层"，要求每个正式脚本都在 scripts/README 登记，防漏登记/堆积。同时补 3 个近期脚本漏登条目。
- **审计缺口**：scripts/README 漏登 `fist-mbt-http.py`、`laya_decide.py`（HTTP 桥接/Laya sidecar）、`showcase.ps1`（R60 视觉演示）。
- **实现**：新增 `scripts/check_scripts_index.py`（R62 守卫，与 check_badge/tools_sync/test_sync 同族）：遍历 scripts/ 全部无 `_` 前缀正式脚本，校验其文件名/去扩展名基名在 scripts/README 出现；漏登→打印清单退出 1。自举（守卫自身也须登记）。
- **修复**：README 补 showcase.ps1 / fist-mbt-http.py / laya_decide.py 条目；守卫自登记条目。
- **CI**：`.github/workflows/ci.yml` JS(ubuntu) 轨加「Scripts-index guard」步，与既有守卫并列作长期门禁——把①②地图/复用之外，把"工具统一管理"也锁进 CI。
- **验证**：`check_scripts_index.py` PASS；正反双向（把 README 撤一版即抓 showcase 漏登=1）；既有三守卫 PASS（83/240 不变，脚本层改动零回归）；cleanup 干净。
- **意义**：支柱③+①汇合——"哪个辅助脚本在哪个文件/干嘛"一眼可查，且由 CI 守护不再漂移。

## 获奖提升 · 第 63 轮：父计划回注（plan_deep reinject_context，ReCAP 借鉴）
> 先调研后落地：派研究子代理调研 AI PM/递归拆解/项目地图 的库与论文，选中最适合本项目（避免与已有 Omega/selfdrive 验证重复造轮）的「父计划回注」增量。
- **调研结论**：① self_review(LLM自验) 会和已有 Omega强验证/selfdrive 重叠，弃做以避免工具冗余（84）；② ReCAP 父计划回注 是既有能力未覆盖的新颖增量 → 选定。
- **实现**：`FistEngine::plan_deep` 增 `reinject_context=false`（默认关闭零回归）；`decompose_rec` 贯通（含递归下钻）；子任务描述追加 `[父计划: {父描述} · 剩余兄弟: {n-i}]`——原子片知道自己"为什么做、旁边还有谁"。MCP `task_plan_deep` 工具增 `reinject_context` 参数透传（工具数仍 83，非新工具）。
- **验证**：engine 51/51（+1：reinject 回注 + 默认关闭零回归，含下钻孙代继承断言）；moon check 0 错误；全量 **241/241**（+1）；badge/test_sync/scripts_index 全 PASS；mcp_smoke 83；cleanup 洁净。
- **联动**：README/AGENTS/deliverable/scoring_rubric 240→241 全量同步；deliverable §四 补 50 行；AGENTS task_plan_deep 行补 reinject_context。
- **调研来源**：ReCAP (Recursive Context-Aware Reasoning and Planning, arXiv:2510.23822)、Reflexion(2303.11366)、Reflection Pattern、repomap-mcp、Flux kanban。

## 获奖提升 · 第 64 轮：父计划回注并入 award_demo 演示链
> 把 R63 的 p1 软增强"变可见"：评审一条命令跑 award_demo 即看到子任务描述带着父计划，知道"为什么做、还有谁"。
- **实现**：`scripts/award_demo.py` ③b 拆解演示段 `task_plan_deep` 加 `reinject_context=True`；新增断言 `[父计划:` 出现在子任务描述，并打印回注片段。
- **验证**：`award_demo.py` **PASS**，输出清晰可见（`T0r159.1 描述含【[父计划: 搭建可插拔的父层调度（拆解演练） · 剩余兄弟: 2]】`）；结尾 cleanup CLEAN，仓库根仅交付库。工具/测试计数不变（83/241）。
- **意义**：R63 能力从"可选参数"升级为评审可一条命令亲眼看到的演示点，直接充值"创意生态/美观演示"维度叙事。

## 获奖提升 · 第 65 轮：门禁复评（R62-64 后）——新档位连续第 3 次全过
> R62-64（脚本索引/父计划回注/演示链）后复评，确认概率兜底不退反进。
- **复评 = PASS=是**：AI1 0.72/0.87/0.97；AI2 0.72/0.87/0.98；**AI3 0.78/0.89/0.975**（三档全升）；AI4 0.71/0.86/0.97。4 家全过 70/85/97。
- **意义**：新档位连续第 3 次全过，概率门禁确认稳健（非随机）；且 R62-64 的"工具统一管理+父计划回注+演示链可见"被评分者 AI3 更正面认可（0.72→0.78 等）。仍按惯例临时快照即用即清（已删 `_snapshot.md`）。
- **留痕**：score-20260924.md 记第 11 次评估（PASS）。
- **后续候选**：概率兜底已多轮稳健，可安心转向其余提升（申报书完善 / 评审 demo 打磨 / 三支柱残留）。

## 获奖提升 · 第 66 轮：临时脚本"任务完即清"策略自动化（cleanup_artifacts 增强）
> 用户点名三支柱③"临时脚本任务完清理策略增强"——把"scripts/ 不留 `_` 前缀临时脚本"落成硬门禁。
- **实现**：`scripts/cleanup_artifacts.py` 新增 `stray_underscore_scripts()`：扫描 scripts/ 下 `_` 前缀的 .py/.ps1/.sh 临时脚本；`--check` 模式把它计入 DIRTY（exit 1），clean 模式移入 temp/ 后统一清理——正式工具目录不留临时脚本，且幂等。
- **验证**：正反双向（造 `_diag_tmp.py` → --check DIRTY；clean → mv 进 temp/ 再删 → 无残留 → --check CLEAN）；既有三守卫 PASS（83/241 不变）；scripts_index 仍登记 cleanup_artifacts（补 README 描述）。
- **意义**：任务完清理策略从"靠自觉"升级为"CI 门禁"——与 check_* 家族、scripts 单一索引形成"工具层整洁"完整闭环（支柱③）。

## 获奖提升 · 第 67 轮：申报书校准（数字漂移 + 实证表补 R56-66）
> 竞赛交付物《项目申报书》出现"文档即实现"漂移：正文仍写 233/233，而实测 241/241；§6 自驱实证表停在 32 行。这是评分时的直接印象分，校准它。
- **校准**：`项目申报书.md` 全局 233→241、deliverable 引用"至第 44 行"→"第 50 行"；§6 实证表补行 33-42（R56-66：引擎层派单 primitive/看门狗自动派单/want 免手传/4-AI 门禁闭环/showcase 自举证据/脚本索引守卫/父计划回注+演示/临时脚本即用即清）；译文基线同步 241/241。
- **产物**：`python scripts/gen_apply_pdf.py` 重新生成 `项目申报书.pdf`（56KB）。
- **边界**：申报书 md/pdf 均为 gitignore 个人参赛件（按政策不入库），本校准仅落本地交付物；无代码/计数变化（83/241）。
- **意义**：申报书与最新能力、测试数逐项一致，消除"文档即实现"在竞赛交付物上的倒退。

## 获奖提升 · 第 68 轮：项目健康卡 `project_health`（新工具，84）
> 深水区新增工具：单次调用看全项目健康，正中支柱①"一目了然"与"更好的 AI 项目管理工具"。
- **实现**：`board_ascii.mbt` 增 `render_project_health(tasks, version)` 纯函数——聚合 in_flight/ready/done/blocked/reviewing/archived 六类计数 + 等级(empty/attention/stalled/healthy) + blocked_tasks；server 注册 `project_health` 工具（schema: namespace 可选），可 ns 过滤。
- **验证**：engine/server check 0 错误；全量 **242/242**（+1：健康卡聚合+等级判定）；mcp_smoke 84 工具；badge/test_sync/tools_sync/scripts_index/map_verify 全 PASS；cleanup 洁净。
- **联动**：工具数 83→84 全量同步（README/AGENTS/deliverable/scoring_rubric + AGENTS 看板组 12→13 + project_health 行 + scripts/README 83→84 + deliverable §四 51 行）。
- **意义**：与 status_summary(pulse) 互补——脉冲看"分布"，健康卡看"一个等级+阻塞详情"，agent 一眼知全局是否可正常推进。

## 获奖提升 · 第 69 轮：project_health 并入 award_demo 演示链
> 把 R68 新工具"变可见"：评审一条命令跑 award_demo 即亲见健康卡。
- **实现**：`award_demo.py` ⑧ 只读段加 `project_health` 调用 + 等级合法性断言 + 打印 grade 与四类计数。
- **验证**：award_demo **PASS**，输出可见 `↳ R68 project_health(award-demo) → grade=healthy；in_flight=55 ready=79 done=14 blocked=0`；结尾 CLEAN。工具/测试计数不变（84/242）。
- **意义**：R68 从"工具存在"升级为评审一条命令亲眼看到的健康卡演示点，直接充值支柱①"一目了然"。与 R63→R64（父计划回注进演示）同一"软能力变可见"模式。

## 获奖提升 · 第 70 轮：门禁复评 FAIL → 触发硬迭代
> R66-69 后复评：**PASS=否**。AI3(默认模型) fail 0.62/0.86/0.96（p1=0.62 显著低于其余三家），其余三家过。诊断：近几轮（软增强/演示可见/整洁）被默认模型视为"锦上添花"，缺硬创新——与门禁纪律一致：**FAIL → 触发自我迭代**，不得重跑赌运气。留痕记第 12 次评估。

## 获奖提升 · 第 71 轮：瓶颈与松弛分析 dag_slack（硬创新，脉冲修复）
> 对症 p1"硬创新"要求：做 PERT/CPM 瓶颈/松弛调度分析，非演示叠加。门禁复评（R70）留出 FAIL 证据 → R71 落地硬核新工具。
- **实现**：engine_dag_ext.mbt 新增 `dag_slack_analysis`（拓扑序 + earliest/latest/slack + 环检测，PERT/CPM）+ `dag_topo_order`（Kahn，前置→后继边）；server 注册 `dag_slack` 工具。返回 { makespan, critical, slack_map, cycle }——"找出谁在关键路径上、谁有松弛可并行"。
- **踩坑**：初版拓扑边方向反转致入度错（indeg counts 前置数，我错加成后继数）、`ready_ids[1..]` 切片非 MoonBit 语法、fold_left 闭包参数不可变、工具描述含未转义双引号——逐一修复。
- **验证**：engine **52/52**（+1：临界/松弛/环检测）；全量 **243/243**（+1）；mcp_smoke 85 工具；工具数 **84→85** + 测试数 242→243 全量同步（README 徽章 %2F 需单独替换、README pipeline 2420 timeout 不动）；tools/test/badge/index 全 PASS；cleanup 洁净。
- **联动**：deliverable §四 52 行；AGENTS DAG 组 13→14 + dag_slack 行；mcp_smoke expected 83→85。

## 获奖提升 · 第 72 轮：门禁复评 PASS——FAIL→硬创新→PASS 闭环成立
> R71 dag_slack 硬创新后复评，验证 p1 短板是否被真硬创新抬回。
- **复评 = PASS=是**：AI1 0.72/0.87/0.97；AI2 0.72/0.86/0.97；**AI3 0.78/0.94/0.99（由 fail 0.62/0.86/0.96 大涨回 pass）**；AI4 0.70/0.86/0.97。
- **意义**：FAIL→硬创新→PASS 闭环成立——PERT/CPM 瓶颈/松弛调度被严格评分者确认为真硬创新（非演示叠加）。门禁纪律（FAIL 不赌运气、对症硬迭代）实证有效。按惯例单次 PASS 需复评一次确认稳健；临时快照已清理。
- **留痕**：score-20260924.md 记第 13 次评估（PASS）。

## 获奖提升 · 第 73 轮：门禁复评确认——新档位连续两次 PASS，兜底重新成立
> R72 单次 PASS 后按惯例复评确认稳健。
- **复评 = PASS=是**：AI1 0.72/0.87/0.97；**AI2 0.76/0.93/0.98（大涨）**；AI3 0.75/0.90/0.98；**AI4 0.74/0.88/0.98（大涨）**。
- **意义**：新档位（70/85/97）连续两次 PASS=是，概率门禁兜底重新成立。dag_slack 硬创新不仅把 AI3 拉回（0.62→0.75+），还让 AI2/AI4 全面升档——真硬创新确凿提升整体评分，非随机。R70 的 FAIL 彻底翻盘。
- **留痕**：score-20260924.md 记第 14 次评估（PASS 确认）。
- **后续候选**：概率兜底重回多轮连续，可安心转申报书/demo 打磨或调研增量。

## 获奖提升 · 第 74 轮：排程视图 dag_schedule（硬创新续，86 工具）
> 概率兜底重新成立后，延续"硬创新抬分"实证路径：把 R71 dag_slack 的松弛分析落成可执行排程视图。
- **实现**：engine_dag_ext.mbt 新增 `dag_schedule`（复用 dag_slack_analysis 不重算图）：按 slack 分 critical_batch(slack=0 瓶颈,须串行盯紧) 与 flexible_batch(slack>0,按最早开始排序可并行,附 assignee)——"谁在瓶颈、谁可并行派单"一目了然；server 注册 `dag_schedule` 工具。
- **验证**：engine **53/53**（+1：分批+assignee）；全量 **244/244**（+1）；mcp_smoke 86 工具；工具数 **85→86** + 测试数 243→244 全量同步（README 徽章 %2F、scoring_rubric "85 MCP 工具"需精确改、p2>=0.85 阈值不动）；tools/test/badge/index 全 PASS；cleanup 洁净。
- **联动**：deliverable §四 53 行；AGENTS DAG 组 14→15 + dag_schedule 行；scripts/README 85→86。
- **意义**：DAG 家族从"静态视图"升级到"可执行排程"——critical_path(最长链)→slack(瓶颈/松弛)→schedule(分批可并行+assignee)，排程优化闭环。

## 获奖提升 · 第 75 轮：排程视图负载感知（既有工具增强，86 工具）
> 延续 R74 排程视图的排程优化闭环：flexible 批里"可并行派给谁"若只附 assignee 现状仍欠具体，R75 补最后一公里——直接给出"建议派单"。
- **实现**：engine_dag_ext.mbt `dag_schedule` 增加负载感知——用 `executor_list` 取已注册执行者，统计其名下活跃(未完成未归档)任务数为负载；flexible_batch 中未认领任务附 `suggest=活跃负载最低的已注册执行者`（无注册则空）。复用 executor_list + 既有状态判断，不重复算图。
- **验证**：engine **54/54**（+1：未认领 flexible 项附负载最低 suggest）；全量 **245/245**；工具数不变（86，R75 是既有工具增强非新工具），测试数 244→245 全量同步（README badges、scoring_rubric、deliverable §二、AGENTS 工具表头）；mcp_smoke 86 工具；tools/test/badge/index 全 PASS；cleanup 洁净。
- **联动**：server.mbt dag_schedule 工具描述(描述里无未转义引号) + 工具数 86 不变但描述 R74→R75；deliverable §四 53 行 dag_schedule 描述追加 + 测试数 245。
- **意义**：DAG 排程家族从"视图"到"可执行排程"再到"具体建议派单"，排程优化真正闭环；与 executor_route/selfdrive_dispatch 同一"按专长+负载分配"动机，一行字段把"沟通用"升级为"可直接照做"。

## 获奖提升 · 第 76 轮：门禁复评 FAIL（AI2 三档全低）→ 触发迭代
> R74/R75 后复评，快照含排程视图+负载感知+三支柱+自举+视觉+调研。
- **复评 = PASS=否**：AI1 0.72/0.87/0.97；**AI2 fail 0.60/0.82/0.95（三档全低）**；AI3 0.72/0.88/0.97；AI4 0.72/0.87/0.97。
- **诊断**：最严苛者 AI2(LongCat) 三档全低，与第 8 次评估同款（0.62/0.82/0.95）——历史 AI2 对「创意生态/视觉演示/三等奖确定性」最敏感，R60 曾靠「视觉演示+自举采用证据」抬回 pass。本轮快照已含这些仍 fail → 需**更强/更新证据**或新维度增量，非项目回归（其余 3 AI 全过且均衡）。
- **调研增量（等待期沉淀）**：2026 下半年成本/市场/拍卖路由论文蒸馏（STAR 依赖图拍卖 / CASTER / ZEBRA / Agora / PROGROUTER / SwarmHarness）→ `memory/research/20260926.cost-market-routing.md` + index 条目，commit `f24eedd`。STAR 式「依赖图成本路由」定为下一硬创新。
- **留痕**：score-20260924.md 记第 15 次评估（FAIL）。临时快照已清理。

## 获奖提升 · 第 77 轮：依赖图成本路由 dag_cost_route（STAR 式硬创新，87 工具）
> 对症 R76 FAIL（AI2 需新证据+视觉）：落地调研蒸馏出的 STAR 式成本路由，DAG 家族最后拼图。
- **实现**：engine_dag_ext.mbt 新增 `dag_cost_route`——对 flexible(slack>0) 未认领任务按拓扑序贪心给建议执行者：执行成本(难度档 易/中/难→1/2/3，复用 extract_difficulty) + 切换成本(依赖执行者不同则 +1，STAR 上下文切换税) + 能力约束过滤(描述命中的已注册能力标签，复用 auto_want)，成本相等取负载最低；纯读不 claim。server 注册 `dag_cost_route` 工具（86→87）。
- **验证**：engine **56/56**（+2：能力约束+切换税+总成本 / 同执行者免切换税+空注册表跳过）；全量 **247/247**（+2）；mcp_smoke 87 工具；工具数 **86→87** + 测试数 245→247 全量同步（README badges/deliverable/scoring_rubric/AGENTS DAG 组 15→16 + dag_cost_route 行/mcp_smoke expected 86→87）；tools/test/badge/index/map 全 PASS；cleanup 洁净。
- **视觉并入**：award_demo ⑧ 后加 R77 段（executor_register + dag_cost_route，实测 476 条建议/total_est_cost=664，PASS）；showcase.ps1 加 4c「成本路由 COST ROUTE」视觉段 + FOOTER 87 tools·247 tests（实测 exit 0）。
- **联动**：deliverable §四 54 行；BACKLOG 锚点同步 86/245（待提交）。
- **意义**：critical_path→slack→schedule→cost_route 排程优化闭环最终形态；机制设计叙事（PERT/CPM 之后接拍卖/成本感知）为门禁新证据——设计源 STAR（arXiv/ACL 2026 依赖图反向拍卖，DSIC+切换成本）。

## 获奖提升 · 第 78 轮：门禁复评确认——新档位连续两次 PASS，兜底成立
> R77 单次 PASS 后按惯例复评确认稳健（连续 PASS 判达标）。
- **复评 = PASS=是**：AI1 0.72/0.87/0.97；**AI2 0.72/0.88/0.97（与 R77 完全一致）**；AI3 0.70/0.87/0.98；AI4 0.71/0.87/0.97。
- **意义**：新档位（70/85/97）连续两次 PASS=是，概率门禁兜底成立。R77 dag_cost_route 硬创新档位稳健无随机性（AI2 两轮复现，AI3/AI4 微浮动均过线）——FAIL→硬创新→PASS 闭环（R76→R77）与 R70→R71 两次实证，门禁纪律完全成立。
- **留痕**：score-20260924.md 记第 17 次评估（PASS 确认）。临时快照已清理。
- **后续候选**：概率兜底重回多轮连续，可安心转申报书/demo 打磨、调研增量（executor_route 历史信任轴 / ZEBRA 背包预算切分）或把门禁 cron 化。

## 获奖提升 · 第 79 轮：全仓计数同步（支柱③ 文档即实现）
> 守卫只覆盖 4 主文档，其余活跃文档计数陈旧（83/85/86、241/245）——评审直接材料尤其申报书是印象分。
- **同步 87 工具 / 247 测试**：ARCHITECTURE(83→87)、docs/agent-map(83→87 ×2)、README Tools 概览(85→87)、USAGE(83→87 ×5)、BACKLOG 锚点(86/245→87/247)、fist://map 资源描述(server.mbt 83→87)、项目申报书(83→87、241→247 全量、§6 至 54 行、补成本路由)；历史 memory/reports 留痕保留不改写。
- **验证**：map_verify PASS、全量 247/247 无回归；申报书为 gitignore 个人档（工作区已同步不入库）。
- **意义**：评审照文档跑命令零矛盾（"文档即实现"从主文档扩展到全部活跃文档）；申报书作为第一印象材料不再显示陈旧数字。

## 获奖提升 · 第 80 轮：历史信任轴 executor_route（SwarmHarness 蒸馏落地）
> 概率兜底稳健后把调研蒸馏转"拿来主义"落地：防"只认领不交付"的信任轴。
- **实现**：executor/registry.mbt 新增 `route_pick_with_trust`（排序键 覆盖 desc → 信任 desc → 负载 asc → 名称 asc，候选带 trust 字段 + basis；原 route_pick 签名保留零回归）；engine_executor.mbt 新增 `executor_trust`（名下已完成/名下总数，无历史 0.5 中性）；server.mbt executor_route 工具改用信任轴（描述 R30→R80）。
- **验证**：executor 7/7（+1：信任轴排序 + 负载回退 + basis）；全量 **248/248**（+1）；工具数 87 不变（既有工具增强）；tools/test/badge/index 全 PASS；cleanup CLEAN。
- **踩坑**：ExecutorRegistry::new() 自带 manual/McpDelegateExecutor 两个系统执行者，candidates 断言长度应为 ≥3 而非 ==3；assert_true 无第二参（编译错），改用 if+abort 携带诊断。
- **联动**：AGENTS executor_route 行 R30→R80；deliverable §四 55 行；README/AGENTS/deliverable/scoring_rubric 测试数 247→248 全同步。
- **意义**：Marketplace 从"专长+负载"升级"专长+信任+负载"——与 SwarmHarness 四轴(能力/负载/延迟/信任)对齐，接住 R77 调研蒸馏的"低成本可测"落地项。

## 获奖提升 · 第 81 轮：预算按依赖图阶段切分 cost_budget_split（ZEBRA 蒸馏，88 工具）
> 接续 R80 的"调研蒸馏转落地"节奏：ZEBRA 背包水填充预算切分——预算在任务图上按阶段切分，瓶颈阶段占额可见。
- **实现**：engine_dag_ext.mbt 新增 `budget_split_by_dag`——按 slack 分析 earliest 层级分阶段，每阶段份额=阶段难度权重(难3/中2/易1，复用 triage_label_of)占总量比例×总预算(向下取整，余数补最大权重阶段)；返回 { stages:[{level,tasks,difficulty_sum,share}], total_budget, makespan, note }。server 注册 `cost_budget_split` 工具（87→88）。
- **验证**：engine **57/57**（+1：阶段切分+份额和=预算+空仓库）；全量 **249/249**（+1）；mcp_smoke 88 工具；工具数 **87→88** + 测试数 248→249 全量同步（README badges/AGENTS 运维组 9→10 + cost_budget_split 行/deliverable §四 56 行/scoring_rubric/mcp_smoke expected 87→88）；tools/test/badge/index 全 PASS；cleanup CLEAN。
- **踩坑**：Map 无 .value() 方法（用 .get）；Map.keys() 返回 Iter 需 .to_array() 才能 sort；var shares 无需（Map set 不需要 mut 声明）。
- **意义**：ZEBRA 蒸馏第二个落地项——"预算按依赖图阶段切分：瓶颈阶段(关键路径层)占额可见，超支先预警"，机制设计叙事续（PERT/CPM→STAR 成本路由→ZEBRA 预算切分）。

## 获奖提升 · 第 82 轮：R80/R81 并入 award_demo 演示链（软可见）
> R60 验证模式：新能力变可见，评审一条命令亲见（对 AI2 等"视觉敏感"评分者有效）。
- **实现**：award_demo.py ⑧ 段追加 R80 executor_route（need=编排，打印 best/trust/load）与 R81 cost_budget_split（budget=100，打印阶段数/makespan）两行演示。
- **验证**：award_demo **PASS**，输出可见 `↳ R80 历史信任轴 executor_route → best=exec_demo trust=0.95 load=1`、`↳ R81 预算阶段切分 cost_budget_split(100) → 3 阶段 makespan=2`；结尾 CLEAN。工具/测试计数不变（88/249）。
- **意义**：R80/R81 从"工具存在"升级为评审一条命令亲眼看到的演示点，机制设计家族（成本路由/信任轴/预算切分）整链可见。

## 获奖提升 · 第 83 轮：门禁复评确认——R77-R82 增量后新档位持续全过
> R77/R78 连续 PASS 后新增 R79-R82 四轮（计数同步/信任轴/预算切分/demo 可见），按门禁纪律"大改后先复评"确认稳健。
- **复评 = PASS=是（第 18 次评估）**：AI1 0.72/0.87/0.97；AI2 0.72/0.87/0.97；**AI3 0.72/0.88/0.98（p2/p3 微升）**；AI4 0.72/0.86/0.97。
- **意义**：机制设计家族（成本路由/信任轴/预算切分）增量不退反进——4 家全过且均衡（p1 全 0.72、p2 0.86-0.88、p3 0.97-0.98）。新档位 70/85/97 持续多轮全过，概率门禁兜底成立且稳健（非随机）。
- **留痕**：score-20260924.md 记第 18 次评估（PASS）。临时快照已清理。
- **后续候选**：概率兜底多轮连续。可转申报书/demo 收尾打磨、调研增量继续落地（Agora 置信度拍卖 / PROGROUTER 进度预算路由）、或把门禁 cron 化。

## 获奖提升 · 第 84 轮：申报书 §6 实证表收尾（机制设计家族入档）
> R83 门禁确认后转申报书收尾：评审直接材料补齐最新功能轮（R68-R81 机制设计家族）。
- **同步**：项目申报书.md §6 实证表补 43-47 行（project_health 健康卡 / dag_slack PERT/CPM / dag_schedule 排程视图 / dag_cost_route 成本路由含门禁抬回证据 / executor_route 信任轴 + cost_budget_split 预算切分含验证脚本）；当前状态 87→88 工具、247→249 测试；引用"至第 54 行"→"至第 56 行"；轮表历史 247/247 记录保留（如实）。
- **验证**：申报书为 gitignore 个人档（工作区已同步不入库）；无代码/测试改动。
- **意义**：申报书作为第一印象材料完整覆盖机制设计家族（PERT/CPM→STAR 成本路由→SwarmHarness 信任轴→ZEBRA 预算切分）与门禁闭环证据，评审读一份文件即见 82 轮自驱增强全貌。

## 获奖提升 · 第 85 轮：README/deliverable 概览收尾（机制设计家族可见）
> 申报书收尾后同步项目内文档：README Tools 概览与 deliverable 调研引用补齐最新能力。
- **同步**：README Tools 概览 DAG 扩展表补 dag_slack/dag_schedule/dag_cost_route 三行、成本表补 cost_budget_split 行（评审读 README 即见机制设计家族）；deliverable §五 调研引用补 20260926.cost-market-routing.md（含已落地 R77/R80/R81）。
- **验证**：check_tools_sync PASS（88 工具对齐）；无代码/测试改动。
- **意义**："文档即实现"从计数对齐升级为能力可见——评审无论读申报书/README/deliverable 都能看到机制设计家族（PERT/CPM→成本路由→信任轴→预算切分）。

## 获奖提升 · 第 86 轮：AIGC 元数据清理 + BACKLOG 锚点对齐（BACKLOG P0 清单）
> 处理 BACKLOG P0 遗留：README/AGENTS 顶部 AIGC 元数据块（避免被误判为纯 AI 生成）+ 锚点计数陈旧。
- **清理**：README.md / AGENTS.md 顶部 `---\nAIGC:...\n---` 元数据块移除（BACKLOG P0 项 done）；BACKLOG 锚点 87/247→88/249。
- **验证**：无代码/测试改动；守卫不影响（README/AGENTS 计数表述未变）。
- **意义**：README/AGENTS 首屏干净直达主题，规避"纯 AI 生成"误判风险（申报书 §7 已如实声明 AI 辅助，元数据冗余反而招疑）。

## 获奖提升 · 第 87 轮：置信度校准拍卖 executor_auction（Agora arXiv 2607.09600 蒸馏落地，89 工具）
> 接续 R80/R81 的"调研蒸馏转落地"节奏：Agora 置信度校准拍卖——把执行者分派从"排序推荐"升级为"按出价竞拍"，防"胜者诅咒"。
- **实现**：
  1. executor/registry.mbt 新增纯计算 `auction_pick`（能力覆盖>0 方可竞拍；出价=显式 bid 或默认按能力覆盖率；校准=1-|出价-历史验收通过率|（防胜者诅咒：过度自信而兑现差的执行者被惩罚）；拍卖分=出价×校准×负载折扣 1/(1+负载)；score desc→名称 asc；返回 bids 全表+winner+basis+note，逐行带 overconfident 标记）；
  2. server.mbt 注册 `executor_auction` 工具（88→**89**，need 必填 + bid 可选出价表 {执行者:0..1}，`json_bid_map` 解析），复用 executor_loads/executor_trust/sync_db_executors 零新依赖。
- **验证**：`registry_test.mbt` 新增 3 测试（能力门槛+默认出价 / 胜者诅咒防护：过度自信 0.9/0.3 败给言行一致 0.6/0.6 / 负载折扣+空 need 无竞拍者）；全量 **249→252/252**（+3）；mcp_smoke 89 工具；award_demo ⑧ 段加 R87 行（竞拍 1 家 winner=exec_demo calibration≈0.943）PASS；守卫族 tools/test/badge/index/map 全 PASS；cleanup CLEAN。
- **踩坑**：core Json 枚举无 `Json::Boolean` 构造子，读布尔字段用 `as_bool()`（`Some(Json::Number(n,..))` 模式正确，Boolean 误用报 4031）。
- **联动**：机制设计叙事续 PERT/CPM→STAR 成本路由→SwarmHarness 信任轴→ZEBRA 预算切分→**Agora 置信度拍卖**；Marketplace 叙事：能力登记→路由(专长+信任+负载)→拍卖(校准出价)。调研档 20260926.cost-market-routing.md 标 Agora ✅ 已落地；README/AGENTS/deliverable/ARCHITECTURE/USAGE(补 §6.14 Marketplace 手册+计数 87→89)/agent-map(能力路由 4→5)/scoring_rubric/mcp_smoke expected 89/BACKLOG 锚点/申报书(§6 补 48 行) 全量同步；fist://map server 计数 87→89（顺带修陈旧点）+ Marketplace 组补 executor_auction。全量 `moon fmt` 顺带做 `var`→`let mut` 弃用现代化（工具链新版）。
- **意义**：Agora 落地让 Marketplace 从"推荐"到"市场清算"——按校准能力而非原始置信度选出赢家，可复现（纯计算）、有论文锚点（2607.09600）、有门禁维度的"机制设计家族"叙事延伸。

## 获奖提升 · 第 88 轮：进度预算路由门控 progress_gate（PROGROUTER arXiv 2608.25992 蒸馏落地，90 工具）
> 接续 R87 的"调研蒸馏转落地"节奏：PROGROUTER 在线进度引导——预算×进度双路径预测 + 元门控决策，兑现调研档最后一块可落地信号。
- **实现**：
  1. engine_dag_ext.mbt 新增 `progress_gate`（纯计算零副作用）：`subtree_ids`（复用 children_of BFS）收集子树；progress=已完成+已归档/子树任务数；spent=子树非待领取任务难度权重 易/中/难→1/2/3（复用 extract_difficulty 单一来源），`spent_override>0` 可手动注入；双路径预测 线性=燃尽率×剩余工作量、保守=1.2×线性；元门控 OK(继续)/CAUTION(降档缩范围)/ESCALATE(追加预算或暂停)。
  2. server.mbt 注册 `progress_gate` 工具（89→**90**，task_id/budget 必填 + spent 可选），复用 jnum 零新依赖；award_demo ⑧ 段加 R88 演示行（实测 verdict=ESCALATE：progress=0.1/spent=8 超支检出，门控真干活）。
- **验证**：`dag_ext_test.mbt` 新增 4 测试（OK 自动估算 spent=2 / CAUTION：spent=3 线性6≤7 保守7.2>7 / ESCALATE+双路径数字 linear=10/conservative=12 / 未开工=OK+全完成=OK+空任务+非法预算 ok:false）；全量 **252→256/256**（+4）；mcp_smoke 90 工具；守卫族 tools/test/badge/index/map 全 PASS；cleanup CLEAN。
- **踩坑**：MoonBit 不接受 `1e-6` 科学计数法浮点字面量（parse error），改用 `0.000001`/`0.000000001`；core Json 无 `Boolean` 构造子继续用 `as_bool()`。
- **联动**：机制设计/预算叙事续 cost_stats→budget_check→budget_split(ZEBRA)→cost_route(STAR)→**progress_gate(PROGROUTER)**，调研档 20260926.cost-market-routing.md 全部 5 信号落地完毕；README/AGENTS(运维·成本组 10→11)/deliverable(R58)/USAGE(§6.8)/agent-map/scoring/mcp_smoke expected 90/BACKLOG 锚点/申报书(§6 补 49 行)/fist://map(server 89→90 + 运维组补 progress_gate) 全量同步。
- **意义**：PROGROUTER 落地让"排程/成本/预算"家族闭环成"预算×进度在线体检"——先预警后决策（计划性增强，正中 @用户建议），纯计算可复现、论文锚点 2608.25992。

## 获奖提升 · 第 89 轮：概率式故障检测 phi_accrual（Hayashibara 2004 经典算法落地，91 工具）
> 20260926.cost-market-routing.md 五信号全落地后开新调研线程（BACKLOG P3）：Phi Accrual 概率式故障检测——把 watchdog/heal 固定 timeout 升级为"按心跳间隔历史分布算怀疑度 φ"。
- **调研先行**：新建 memory/research/20260926.reliability-phi-accrual.md（Hayashibara 2004 IEEE/SRDS，Cassandra/HBase 生产采用；对照 fixed/adaptive timeout），蒸馏出 φ 计算原语设计草案。
- **实现**：engine_phi.mbt 新增自由 pub 函数 `phi_accrual`（纯计算零副作用，同 extract_difficulty 模式）：
  - φ = -log10(P(心跳晚于 elapsed 到达))；窗口内间隔均值 μ + 样本标准差 σ（Bessel 校正）；
  - σ≈0 回退指数分布 φ=elapsed/μ·log10(e)；正态建模 z=(elapsed-μ)/σ，|z|<3 用 A&S erf 近似、|z|≥3 用尾部渐近展开保深尾部精度（erf 在深尾部误差放大）；
  - core 无 sqrt → 牛顿法自实现 `sqrt_approx`；engine moon.pkg 加 moonbitlang/core/math 依赖（exp/log10）；
  - φ≥threshold(默认 8，原论文口径) 判 suspect 否则 healthy；无间隔历史 insufficient；尾部下溢饱和 999。
  - server 注册 `phi_accrual` 工具（90→**91**，intervals 秒数组 + elapsed 必填 + threshold 可选，`intlist` 助手解析）。
- **验证**：engine_phi_test.mbt 新增 3 测试（σ=0 指数回退：固定 5s elapsed 5s→φ≈0.434 健康 / 100s→φ≈8.686 怀疑；正态建模 μ=10/σ≈3.54：z=5→φ≈6.5 健康 / z=6→φ≈9.0 怀疑；单调性 elapsed 越大 φ 越大 + 空历史 insufficient）；全量 **256→259/259**（+3）；mcp_smoke 91 工具；award_demo ⑧ 段加 R89 演示行（实测 φ=44.98 verdict=suspect：6 倍心跳间隔未到，概率检出超时）PASS；守卫族 tools/test/badge/index/map 全 PASS；cleanup CLEAN。
- **联动**：README/AGENTS(运维组 6→7 + phi_accrual 行)/deliverable(R59)/USAGE(§6.3)/agent-map(运维 6→7)/scoring/mcp_smoke expected 91/BACKLOG 锚点 + Phi P3 行 done/申报书(§6 补 50 行)/fist://map(server 90→91 + 运维组补 phi_accrual) 全量同步；调研档 20260926.reliability-phi-accrual.md ✅ 落地。
- **意义**：可靠性工程新能力维度（非又一个查询/路由工具，是看护族判活信号源升级）——"心跳节奏越快、越久没来才值得怀疑"的概率化看护，有经典论文锚点 + Cassandra/HBase 工业采用证据，硬创新密度续。

## 获奖提升 · 第 90 轮：概率式看护闭环 watchdog phi_gate（R89 原语 → 运行时行为，261 测试）
> 接续 R89：把 phi_accrual 从"独立原语"升级为"运行时行为"——心跳间隔历史持久化 + watchdog 端到端 φ 判活（默认关闭零回归），可靠性线程闭环：原语→持久化→运行时看护。
- **实现**：
  1. store_sqlite 新增 `heartbeat_history` 表（每任务保留最近 1000 条间隔）+ `write_heartbeat_interval`/`list_heartbeat_intervals`（时间序读回）+ `clear()` 一并清空（防测试/scratch 残留，对齐 R11/R31 纪律）；StoreBackend 双后端 dispatch（Mem=no-op，与心跳表同模式）；engine 两个 wrapper。
  2. server `heartbeat` 工具：每次上报用 `@ops.iso_to_secs` 算本次间隔（now-prev，可解析且 >0 才记）落库——间隔历史自动累积，AI 无需手填。
  3. ops_heal 新增 `heal_stale_tasks_by_phi`：对活跃任务读持久化间隔历史 + elapsed，`@engine.phi_accrual` 判 suspect 才回滚；从未心跳沿用 no_signal 语义；无历史/时间戳不可解析/时钟回拨保守不判。
  4. ops_watchdog `watchdog_tick` 加 `phi_gate`（默认 false 零回归）+ `phi_threshold`（默认 8）：true 走 φ 判活路径，detail 挂 phi_gate 标志；server watchdog_tick 工具 schema/handler 同步 + 顺带把 autodispatch/autodispatch_want 透传（R57 能力此前 MCP 不可达，默认 false 零回归）。
- **验证**：`ops_watchdog_test.mbt` 新增 2 测试（间隔史写入/时间序读回/clear 清空 + phi_gate 对照：静默 100s、间隔 [5,5,5]、固定 timeout 600 不判 vs φ≈8.686>8 判死回滚）；全量 **259→261/261**（+2）；守卫族 tools/test/badge/index/map 全 PASS；mcp_smoke 91 工具（计数不变，行为增强）；cleanup CLEAN。
- **踩坑**：`let mut rows` + `rows.push` 报 unused_mut（MoonBit push 不需 mut），去 mut；autodispatch detail 重建段需补 phi_gate 字段防丢。
- **联动**：README/AGENTS/USAGE watchdog_tick 行补 phi_gate/autodispatch 参数；deliverable R60；BACKLOG Phi P3 行 R89+R90 全 done；申报书 §6 补 51 行。可靠性叙事闭环：固定 timeout → 概率怀疑度 φ（R89 原语）→ 心跳历史持久化 + watchdog φ 判活（R90 运行时）。
- **意义**：概率式看护从"能算 φ"到"真用 φ 判活"——不规则慢心跳不再被固定超时误杀，看护族（heartbeat→phi_accrual→watchdog phi_gate）全链路闭环，默认关闭零回归守护既有语义。

## 获奖提升 · 第 91 轮：第 19 次门禁复评（FAIL×2→对症→PASS · 通道修复 + 申报书校准，91 工具/261 测试）
> R84-R90 四轮硬创新后按门禁纪律大改复评。四跑并述，暴露两个真实问题并逐一对症：
- **门禁基建**：score_gate.py 修 Windows 长提示词截断（cmd 8191 限制）——先试 base64 单行 + argv 直连（CreateProcess 32767 上限），Run3 实证 AI3 在该通道下失败（模型去解码→调工具→无工具→放弃）；改用 atomcode 原生 `--prompt-file`（提示词落文件直读 + `--no-tools` 禁工具循环）→ Run4 AI3 error→0.76/0.91/0.99 全档最高。**教训：传输通道要选模型零负担的原生机制，别让模型"解码"长提示词。**
- **p3 真实短板**：AI2/AI4 p3 连跑 0.92-0.95 <0.97（三等确定性证据不足）。复核发现**申报书表头陈旧计数（88 工具/249 测试，实为 91/261）**——"文档=实现"硬约束违规，实时校准 + PDF 重生成（gen_apply_pdf.py）+ 快照补提交包完备性；Run4 AI2/AI4 p3 双双抬回 0.97。**教训：评审文档的当前状态计数是门禁 p3 的隐性扣分点，每轮工具/测试数变化必须全量同步。**
- **Run1-4 分数**：Run1 AI2 pass 0.92/0.90/0.97（AI3/4 timeout）；Run2 AI2 fail 0.98/0.96/0.94；Run3 AI2 fail 0.82/0.86/0.92 + AI3 error + AI4 p3=0.95；**Run4 全过（AI1 0.73/0.88/0.98 · AI2 0.72/0.86/0.97 · AI3 0.76/0.91/0.99 · AI4 0.72/0.88/0.97）PASS=是**。
- **验证**：moon test js 261/261 实测；check_tools_sync（91）/check_test_sync（261）/check_badge（README 261%2F261==实测）全 PASS；快照 _snapshot.md 强化（§一b + 文档校准 + 提交包完备性）。
- **意义**：门禁纪律闭环第四次实证（FAIL→诊断→对症→PASS）；文件通道（--prompt-file）定为门禁标准传输；申报书校准消除"文档失真"淘汰风险——获奖概率兜底 70/85/97 恢复多轮连续全过。

## 获奖提升 · 第 92 轮：Saga 补偿事务（Garcia-Molina & Salem 1987 蒸馏，93 工具/265 测试）
> BACKLOG P3「Saga 补偿事务 + durable action log」落地——机制设计家族补上"失败补偿"维度：多步骤任务链失败不再卡死/整树重来，按 LIFO 优雅收尾。
- **调研先行**：新建 memory/research/20260926.saga-compensation.md（Sagas 1987 SIGMOD 论文锚点 + Temporal/Step Functions/Cadence/DBOS 工业对标；补偿=业务逆转非 ROLLBACK、LIFO 严格倒序、补偿幂等契约、pivot 点、orchestration 风格与指挥官定位一致）。
- **实现**：
  1. store_sqlite 新增 `saga_log` 表（append-only，UNIQUE(ns,root_task_id,step)）+ `write_saga_action`（幂等重登记重置 pending）/ `list_saga_actions`（前向序）/ `mark_saga_actions_done`（IN 子句批量，空 ids 短路）；`clear()` 一并清空（防测试/scratch 残留）；StoreBackend 双后端 dispatch（Mem=no-op）+ engine wrapper ×3。
  2. engine_saga.mbt（新）：`saga_register`（登记可补偿动作，返回 registered JSON）/ `saga_rollback`（只取 pending 按 id 倒序=LIFO 返回序列；mark=true 默认返回后即标 done，幂等防重复回滚；mark=false 仅预览）。
  3. server 注册 `saga_register`/`saga_rollback` 两工具（**91→93**，运维组 7→9，fist://map 同步）。
- **验证**：`engine_saga_test.mbt` **+4**（LIFO 严格倒序 + 默认 mark done + 幂等 / 同键幂等重登记 compensation 更新 / mark=false 预览不消费 / clear 清空）；全量 **261→265/265**；mcp_smoke 93 工具；守卫族 tools/test/badge/index 全 PASS（AGENTS/README/deliverable/scoring_rubric 93/265 对齐）；award_demo PASS（结尾 cleanup CLEAN）。
- **踩坑**：SQLite `column_text` 返回 Bytes 需 `str_from_bytes`；Json bool 构造子是 `Json::boolean`（非 bool/Bool）；可选命名参数带 `~` 的是 label 参数（saga_rollback 的 root_task_id 无 `~` 为位置参数，调用 `e.saga_rollback("R1")`）；`let mut` + `push` 报 unused_mut（MoonBit push 不需 mut）；字符串内嵌 ASCII 双引号会断串（用「」）。
- **联动**：README（徽章 265%2F265 + Tools 93 + 全量计数）/AGENTS（93 tools + 运维 9 行）/deliverable（93/265）/USAGE（93 手册）/scoring_rubric（93/265）/mcp_smoke expected 93/BACKLOG Saga 行 done/申报书（本地 93/265 + §6 行 52）。
- **意义**：硬创新密度续——事务性补偿语义有 1987 经典论文锚点 + 工业工作流引擎（Temporal 等）生产采用证据；机制设计家族叙事闭环：拆解→排程→预算→派单→看护→**补偿**；"更好的 AI 项目管理工具"再添"失败优雅收尾"维度。

## 获奖提升 · 第 93 轮：门禁复评确认 R92（第 20 次评估全过 · --prompt-file 默认通道零超时）
> R92 大改后按门禁纪律复评确认。首次把 `--prompt-file` 固化进 score_gate.py 默认模板（R91 遗留建议：AI2=LongCat-2.0/longcat、AI3=默认、AI4=kimi-k3/Kimi，零 env 依赖），一条命令跑门禁。
- **复评 = PASS=是（4 AI 全过）**：AI1 0.73/0.88/0.98 · AI2 0.70/0.86/0.97 · AI3 0.72/0.89/0.98 · AI4 0.72/0.86/0.98。
- **关键实证**：① **三家 CLI 零超时零 error**——`--prompt-file` 默认通道彻底根除 R91 的截断/解码/工具循环基建问题；② R92 Saga 增量被 4 AI 正面认可（p1 全 0.70-0.73 / p2 0.86-0.89 / p3 0.97-0.98）；③ AI2/AI4 p3 连续两轮 ≥0.97——p3 证据（文档=实现校准 + 提交包完备性）加固后稳健无临界随机性。
- **验证**：快照 _snapshot.md 已随 R92 更新为 93/265；score_gate.py 默认模板改造（无回归：env 覆盖仍生效）。
- **意义**：门禁纪律闭环第五次实证（大改后复评确认：R92 硬创新→全过）；概率兜底 70/85/97 连续多轮全过，获奖概率判定维持达标。

## 获奖提升 · 第 94 轮：Monte Carlo 概率式完工预测 dag_mc（Van Slyke 1963 蒸馏，94 工具/269 测试）
> PERT 家族确定性分析（dag_slack，R71）的概率式补充——回答"能不能按期、风险在哪"，硬创新续。
- **调研先行**：Van Slyke 1963《Monte Carlo methods and the PERT problem》首倡 MCS 求网络完工 CDF；PERT 三点估算（TE=(O+4M+P)/6）的确定性局限（单关键路径/merge bias）由整网模拟克服；工业对标 Primavera Risk Analysis / Crystal Ball；关键度=任务出现在最长路径的频率（含近关键路径）。
- **实现**：engine_dag_mc.mbt（新）：
  1. xorshift32 确定性 PRNG（32 位掩码保证 js/native 双 target 数值一致）+ 牛顿法 mc_sqrt（core 无 sqrt）+ 三角分布采样（难度档→(a,m,b)：易(2,3,5)/中(3,5,8)/难(5,8,14)，缺省按中）；
  2. mc_scope_tasks：root_task_id 为空取整 ns，否则取子树（parent 链传递闭包，环保护）；
  3. mc_finish：单样本内最长完成时间（沿 depends_on 回溯，memo 化 DFS + 环保护）；
  4. `dag_mc(ns~, root_task_id?="", deadline?=0, samples?=1000, seed?=42)`：整网模拟 N 次 → makespan 分布(min/mean/p50/p90/max) + P(≤deadline) + criticality 排行(top 10，任务出现在最长路径的频率)。
- **验证**：dag_mc_test.mbt **+4**（同 seed 可复现 + p_on_time 随 deadline 单调（紧 3 < 宽 100，宽=1.0）/ 关键度——A→B 链 B freq≈1 旁路 C 不关键 / 空作用域 insufficient / samples<10 钳到 10）；全量 **265→269/269**；mcp_smoke **94** 工具（重新 build 后，旧 main.js 93 需 moon build 刷新）；守卫族 tools/test/badge/index 全 PASS（94/269 对齐）；award_demo PASS（cleanup CLEAN）。
- **踩坑**：core 无 `@math.sqrt`（牛顿法自实现）；`t.parent_id` 是 `String?`（需 match）；只有 labelled 参数可带默认值（`root_task_id? : String = ""`）；Array tuple 的 `sort(cmp)` 不可用（手写选择排序降序）；`let mut` + push 报 unused_mut。
- **联动**：README（徽章 269%2F269 + Tools 94 + 全量计数）/AGENTS（94 tools + DAG 组 16→17 + dag_mc 行）/deliverable（94/269）/USAGE（94 手册）/scoring_rubric（94/269）/mcp_smoke expected 94/BACKLOG 锚点 94/269/申报书（本地 94/269 + §6 行 53）。
- **意义**：机制设计家族叙事闭环继续：拆解→排程(PERT/CPM 确定性 dag_slack + **概率式 dag_mc**)→预算→派单→看护→补偿——"更好的 AI 项目管理工具"从"谁在关键路径"升级到"能不能按期、风险概率多高、谁是最大风险"。
